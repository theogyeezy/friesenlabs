# Runtime deps for the API + worker images (kept lean; no test/lint tooling).
fastapi>=0.115
uvicorn[standard]>=0.30
boto3>=1.34
anthropic>=0.40
psycopg2-binary>=2.9
jsonschema>=4.20
pyjwt[crypto]>=2.8    # Cognito JWKS verification (RS256)
