# Uplift — TODO

## Lane Nick — completion status (2026-06-10)

Most of this file's remaining `- [ ]` are **done**, **owner-gated**, **cost-parked**, or **Lane Matt's**.
Live-verified Nick-lane work this cycle (checked off below): cube deploy + model + Cloud Map,
provisioning Lambda + pinned SFN, CI/CD pipeline proven end-to-end (prod on current `main`), rotation
executed, X-Origin-Verify edge lockdown, observability (alarms + dashboard + SNS confirmed), audit
logging, GuardDuty/Config, **CloudFront WAF + access logging + HSTS + PriceClass_100**, **Cognito
deletion-protection + admin-create-only + 7-day refresh**, **AWS provider pin**, **ECS circuit
breakers**, IAM tightening (exact-ARN api task secrets, de-wildcarded SFN), xray perms, lock file,
shared log-retention, `.stignore` parity, the GHL mobile-first landing.

The unchecked remainder, by category:
- **Owner-gated (only the account owner can act):** the MA environment key `uplift/env-key`
  (Anthropic Console "Generate environment key" — no SDK mint; unblocks the worker deploy + its SG
  egress); the friesenlabs.com **NS cutover** at Squarespace (unblocks ACM validation → ALB TLS
  cutover → CF→ALB https origin → drop the :80 rule → retire api_cdn → CF min-TLS 1.2 — TODO
  131/166/210/211/226/227/231); the **Stripe webhook secret** + **Anthropic admin key** (signup
  go-live). All wired; each goes live the moment its value/click lands.
- **Cost-parked vs the $200 ceiling (flip when the ceiling moves):** API→2 tasks + autoscaling,
  Redis HA/AUTH, budget deny-at-90% action (blast radius), NAT→VPC-endpoints (SAFE-NOW lever),
  Aurora min-ACU/auto-pause, Fargate right-size/Spot. PriceClass_100 already done.
- **Maintenance-window (reboot/replace):** Aurora `rds.force_ssl=1` (cluster reboot), cube-api-secret
  rotation, ECR lifecycle policy, ALB-SG 443-dead-rule + egress scoping, GH Actions SHA pinning.
- **Lane Matt (app code) / architecture decisions:** the AI-plane runtime methods + conversation
  factory + tool executor (`conv/`, `agents/`), `agent_plane.ensure()` eager-vs-lazy, Python dep
  pinning, app-side `sslmode=verify-full`, and the MA architecture questions (HIPAA path, 100-workspace
  ceiling, custom-tool path, self_hosted resource limits, heartbeat cadence).


_Last updated 2026-06-09. Synthesized from 5 dimension audits (frontend-auth, ai-agent-plane, infra-deploy-drift, integrations-provisioning, security-ops-cost), grounded in the live repo + state._

## Status at a glance — live / demo / not live

| State | Component | Why it's in this state | Unblocked by |
|-------|-----------|------------------------|--------------|
| ✅ **Live & working** | Amplify hosting → CloudFront → ALB → arm64 Fargate API → Aurora (FORCE'd RLS) | deployed + **verified** (`/api/healthz` 200) | — |
| ✅ **Live & working** | Cognito JWKS auth on the API | enforced + verified (`/api/approvals` → 401 unauth) | — |
| ✅ **Live & working** | Terraform state in S3 (KMS), $200 CLI budget alarm | applied | — |
| ✅ **Live & working** | Web UI with real login (Hosted-UI PKCE, `web/src/auth/`) | real-mode build deployed; **browser-verified**: gate → Hosted UI → code exchange → app shell → real RLS-scoped rows | — |
| ⛔ **Not live** | AI / agent plane (chat, tool execution) | `agents/runtime.py` stub raises; `/chat` → graceful 503; executor is a noop | **Anthropic Managed Agents creds** |
| ⛔ **Not live** | cube + worker services, observability, cortex retrain | authored + `validate`-clean, **not applied** | deploy |
| ⛔ **Not live** | Provisioning (Stripe checkout/webhook, Resend email, signup) | `prod_deps` wires `_Stub`/`_Noop`; email/SMS verify hardcoded off | **Stripe / Resend / Admin creds** |
| ✅ **Reconciled** | Terraform state | out-of-band SG rules imported; ECR back to IMMUTABLE; full plan = 0 change/destroy to live resources | — |
| ✅ **Hardened** | Tenant isolation under concurrency | **critical** shared-connection cross-tenant leak FIXED (pooled per-request conns + `SET LOCAL`); proven on live Aurora (16 threads), CI real-Postgres, local. Aurora durability on. | — |
| 🟡 **Open** | Security-audit backlog | 27 findings (1 critical FIXED, 2 high [1 fixed], 7 med, 17 low) — see the Security audit section below | work the high/medium items |

## What is LIVE today (verified 2026-06-09)
- **The product end-to-end, including login** — browser-verified: sign-in gate → Cognito Hosted UI (PKCE) → code exchange → app shell → real RLS-scoped tenant rows. Path: `browser → Amplify (real mode) → CloudFront (d1vw20lc120dpa.cloudfront.net) → ALB (HTTP) → arm64 Fargate API (1 task) → Aurora`.
- Real Cognito JWKS auth (unauth → 401); `/chat` → graceful 503 (AI parked). Demo user creds in Secrets Manager `uplift/demo-user`; tenant seeded via `scripts/seed_demo_tenant.py`.
- VPC/NAT/SGs, IAM, Secrets, ECR (IMMUTABLE), S3, Aurora, Redis, Cognito (+Hosted UI domain), CloudTrail, Step Functions skeleton, ECS cluster, CloudFront API edge, Amplify Hosting. Terraform state in S3 (KMS), reconciled.

## What is PARKED / NOT REAL YET
- **AI/agent plane stubbed**: `agents/runtime.py` `ManagedAgentsRuntime` methods raise `NotImplementedError`; `/chat` 503 (`conversation_factory` returns `None`); executor is a noop.
- **Provisioning clients are stubs**: `api/prod_deps.py` wires `_StubStripe`, `_StubCognito`, `_Noop` (email/SMS/admin/agent_plane); `email_token_ok`/`sms_code_ok` hardcoded `False`; `_AccountStore` is in-memory.
- **Authored-but-unapplied modules**: cube, worker, observability, provisioning Lambda, cortex retrain target.
- **No domain/TLS-at-origin**: HTTPS is via the CloudFront default cert over a plaintext-HTTP ALB; the ALB is internet-facing with only the shared CloudFront prefix-list as ingress (no shared-secret header).

---

## Critical path to a fully-real product (P0 sequence)
1. ~~**Stand up the login flow**~~ ✅ DONE (2026-06-09) — Hosted UI domain + OAuth client config, `VITE_COGNITO_*` env, `web/src/auth/` PKCE client + callback + AuthProvider + gate, ID token attached, `VITE_API_MOCK=0` deployed, browser-verified live.
2. ~~**Make a user real**~~ ✅ DONE — demo user with `custom:tenant_id` (creds in `uplift/demo-user`), tenant seeded in Aurora (4 companies / 6 contacts / 6 deals / 5 activities / 3 approvals) as `crm_app` under RLS.
3. ~~**Reconcile state before any apply**~~ ✅ DONE — SG rules imported, budget untainted, ECR IMMUTABLE, plan = 0 change/destroy to live resources (`infra/prod.auto.tfvars` carries live var values, gitignored).
4. **Close the origin** — add a CloudFront→ALB shared-secret header (the ALB is currently open to any CloudFront customer).
5. **Harden data** — Aurora backup retention ≥7d + deletion protection + final snapshot; run `isolation_test.py` against LIVE Aurora as `crm_app`.
6. **Unpark the AI plane** (needs Anthropic creds) — verify MA SDK shapes, implement runtime methods (env/agent/coordinator/session/vault), persist per-tenant `coordinator_id`/`environment_id`, wire `conversation_factory` (fixes `/chat` 503), build the real executor, deploy the worker.
7. **Unpark provisioning** (needs Stripe/Resend/Admin creds) — real Stripe + webhook secret, Aurora-backed account store, email/SMS verification, Resend/Cognito-admin/Anthropic-Admin clients, deploy the provisioning Lambda + wire the webhook to Step Functions.
8. **Operate** — deploy observability alarms + SNS, fix the $200/$500 budget + subscriber, WAF on the edge, CI/CD deploy pipeline.

---

## Frontend & Auth

### P0
- [x] **Add Cognito Hosted UI domain to the auth module** — no `aws_cognito_user_pool_domain` exists; without it there's no `/authorize` + `/token`. Add a prefix domain and output the hosted-ui base URL. _(P0 · blocked: none (custom-domain variant needs domain purchase) · `infra/modules/auth/main.tf` · done when: `https://<prefix>.auth.us-east-1.amazoncognito.com/login` returns 200)_
- [x] **Configure OAuth flows/scopes/callback+logout URLs on the app client** — deployed client has empty `allowed_oauth_flows/scopes/callback_urls`; set `code` flow + `openid email profile` + SPA+localhost callbacks (PKCE, no secret since `generate_secret=false`). _(P0 · blocked: none · `infra/modules/auth/main.tf` · done when: `/authorize?response_type=code` shows a login page, not redirect_mismatch)_
- [x] **Thread SPA redirect URLs into TF as variables** — declare `web_callback_urls`/`web_logout_urls` once; don't hardcode the live Amplify/CloudFront subdomain in module source. _(P0 · blocked: none · `infra/modules/auth/main.tf`, `infra/main.tf`, `infra/variables.tf` · done when: plan accepts the var; no live hostnames hardcoded in module)_
- [x] **Expose `VITE_COGNITO_*` to the SPA at build time** — add domain/client-id/region (+scopes/redirect) to Amplify `environment_variables`, sourced from `module.auth` outputs. _(P0 · blocked: Hosted-UI domain + client config · `infra/modules/web_hosting/main.tf`, `infra/main.tf` · done when: `import.meta.env.VITE_COGNITO_CLIENT_ID` is defined in the build)_
- [x] **Build the SPA PKCE authorization-code client** — no sign-in code exists in `web/src`. Create `web/src/auth/cognito.ts` (PKCE verifier+challenge, `/authorize` redirect, `/oauth2/token` exchange). Hand-rolled or `oidc-client-ts`; do NOT pull full `aws-amplify`. _(P0 · blocked: Hosted-UI domain + client config · `web/src/auth/cognito.ts`, `web/package.json` · done when: `signIn()` navigates to `/authorize` with a `code_challenge`; `exchangeCodeForTokens()` returns id/access/refresh tokens)_
- [x] **Implement the OAuth callback route + token exchange** — `main.tsx` only routes `?view=`; add a `?code=`/`/auth/callback` handler that validates `state`, exchanges the code, stores tokens, strips `?code` via `history.replaceState`. _(P0 · blocked: SPA auth module · `web/src/main.tsx`, `web/src/auth/cognito.ts`, `web/src/auth/Callback.tsx` · done when: real login renders the authed shell; `?code` is removed from the URL)_
- [x] **Add a sign-in gate so unauthenticated users get a login button** — `App()` renders the full shell unconditionally and `landing.tsx` "Sign in" is a dead `<a href=index.html>`. Gate on token; wire landing + `SignupFlow.goToLogin()` to `signIn()`. _(P0 · blocked: SPA auth module · `web/src/app.tsx`, `web/src/main.tsx`, `web/src/screens/landing.tsx`, `web/src/signup/SignupFlow.tsx` · done when: no-token load shows Sign in → Hosted UI)_
- [x] **Token storage + AuthProvider/context** — store tokens (document XSS tradeoff) and expose `{idToken, claims, tenant_id, email, isAuthenticated, signOut}`. Replace the hardcoded "Jordan Reyes" / `localStorage 'fl_me'` chip with real claims. _(P0 · blocked: callback + token exchange · `web/src/auth/AuthContext.tsx`, `web/src/app.tsx`, `web/src/main.tsx` · done when: `useAuth()` returns claims; topbar shows the real email; `tenant_id` available)_
- [x] **Attach the Cognito ID token to `/api` and remove `VITE_API_TOKEN`** — `client.ts` reads a static `VITE_API_TOKEN` (line 194) and sends one fixed Bearer (line 370). Make the token dynamic per-request from AuthContext; send the **ID token** (`token_use=id`, `aud=client_id` per `api/auth.py`). _(P0 · blocked: AuthProvider · `web/src/api/client.ts`, `web/src/auth/AuthContext.tsx`, `infra/modules/web_hosting/main.tf` · done when: `/api/approvals` carries the id_token → 200 logged-in, 401 signed-out; no `VITE_API_TOKEN` read)_
- [x] **Flip `VITE_API_MOCK=0` and confirm real surfaces** — `web_hosting` sets mock=1 when `api_base_url==""`; set the base URL so the ApiClient hits `/api`. Handle `/chat` 503 gracefully. _(P0 · blocked: ID-token attach + a real user + seeded tenant · `infra/modules/web_hosting/main.tf`, `infra/main.tf`, `web/src/api/client.ts` · done when: `defaultClient().isMock()` is false in prod; Greenlight renders live rows)_
- [x] **Create a real Cognito user with `custom:tenant_id`** — no pool user exists; add a repeatable boto3 script (admin_create_user + permanent password + set immutable `custom:tenant_id` admin-side, mirroring `provisioning.cognito.set_tenant_id`). Keep creds out of the repo. _(P0 · blocked: none · `scripts/seed_cognito_user.sh`, `signup/provisioning.py` · done when: `admin-get-user` shows CONFIRMED + `custom:tenant_id`; ID token carries the claim)_
- [x] **Seed a tenant's Aurora data so login shows rows** — schema has tenant_id+RLS but no demo seed; the seed tenant UUID must equal the user's `custom:tenant_id` or RLS returns nothing. One-off Fargate task (like `api.migrate`) inserting deals + a pending approval + a saved view. _(P0 · blocked: none · `db/seed.sql`, `scripts/seed_tenant.sh`, `api/migrate.py` · done when: with `app.current_tenant` set, counts >0; Greenlight/dashboard show rows)_

### P1
- [x] **Implement sign-out (Hosted UI logout + local clear)** — `app.tsx` "Sign out" is a static `<a href=Home.html>`. Clear tokens, redirect to Cognito `/logout?...logout_uri=<spa>`. _(P1 · blocked: AuthProvider + logout_urls configured · `web/src/app.tsx`, `web/src/auth/cognito.ts`, `web/src/auth/AuthContext.tsx` · done when: Sign out clears tokens, lands on `/logout`, re-prompts on reopen)_
- [x] **Refresh-token handling + silent renew** — only `ALLOW_REFRESH_TOKEN_AUTH` is enabled; renew at `/oauth2/token` (`grant_type=refresh_token`) before id_token TTL and on a 401-then-retry. _(P1 · blocked: callback + token exchange · `web/src/auth/cognito.ts`, `web/src/auth/AuthContext.tsx`, `web/src/api/client.ts` · done when: an `/api` call still succeeds after id_token TTL elapses)_
- [x] **Central 401/expired-session handling** — `ApiClient.request` throws `ApiError` but nothing reacts app-wide; surfaces render raw `e.message`. On 401 (after refresh fails) clear session + route to Sign in with a "session expired" message. _(P1 · blocked: AuthProvider + refresh · `web/src/api/client.ts`, `web/src/auth/AuthContext.tsx`, `web/src/api/GreenlightQueue.tsx`, `web/src/api/DashboardView.tsx` · done when: a forced 401 redirects to Sign in, no raw "API 401" string)_
- [x] **Loading/empty/error states under real data** *(#33 + #38 + integrations panel #78)* — ChatDock must show "Agents unavailable" on `/chat` 503; empty tenant → "Inbox zero"; Aurora cold-start spinners must show. Audit each error branch vs 401/403/500/503/network. _(P1 · blocked: `VITE_API_MOCK=0` flip · `web/src/api/ChatDock.tsx`, `web/src/api/GreenlightQueue.tsx`, `web/src/api/DashboardView.tsx` · done when: no raw "API <code>" reaches the user)_
- [x] **Mount the api-wired surfaces in the real shell (not just `?view=` seams)** *(#33/#38 swaps + #78)* — `App()` still renders prototype screens backed by in-memory `FLStore`; the ApiClient surfaces are only reachable via `?view=`. Back primary nav (approvals/dashboard/ask) with the ApiClient. _(P1 · blocked: `VITE_API_MOCK=0` + ID-token attach · `web/src/app.tsx`, `web/src/screens/greenlight.tsx`, `web/src/screens/chat.tsx`, `web/src/api/GreenlightQueue.tsx` · done when: sidebar nav shows API-backed rows, not FLStore demo data)_

### P2
- [x] **Add a `/me` endpoint or document claim-only bootstrap** *(GET /me + /api/me @#42)* — no `/me` route in `api/app.py`; either add an authenticated `GET /api/me` returning `{email, tenant_id, name}` or document claim-only bootstrap as deliberate. _(P2 · blocked: none · `api/app.py`, `web/src/auth/AuthContext.tsx` · done when: `/api/me` 200/401, or a documented decision)_
- [x] **Playwright e2e for auth gate / callback / 401** *(offline auth.spec.ts @#66)* — e2e covers mock surfaces but nothing for auth; add offline-stubbed specs (unauth→Sign in, stubbed callback→authed shell, simulated 401→Sign in). _(P2 · blocked: SPA auth module + callback · `web/e2e/auth.spec.ts`, `web/src/auth/cognito.ts` · done when: `npm run test:e2e` includes a green offline `auth.spec.ts`)_
- [x] **Remove demo/test affordances from the prod bundle** *(#66: real builds drop window.claude stub, __pwned hooks, demo assets; grep-proven)* — strip mock/seed data, `window.claude` stub, Playwright `window.__pwned` hooks, and the matt-yee/nick-friesen demo assets in `web/public/` from the shipped build. _(P2 · blocked: Cognito login flow · `infra/modules/web_hosting/main.tf`, `web/src/store.tsx`, `web/src/data.tsx`, `web/e2e/safehtml.spec.ts`, `web/public/` · done when: grep of `dist` is clean of mock/seed/`__pwned`)_

### P3
- [x] **Document the auth env contract; purge `VITE_API_TOKEN`** — document `VITE_COGNITO_*` + redirect URI; update README/BUILD_STATUS notes that claim "mock-only for lack of login". _(P3 · blocked: ID-token attach + mock flip · `web/README.md`, `web/CONVERSION_NOTES.md`, `BUILD_STATUS.md`, `CLAUDE.md` · done when: `grep -r VITE_API_TOKEN` is empty; docs updated)_

---

## AI / Agent plane

### P0
- [x] **Verify MA SDK shapes before any live call** *(done live by Lane Nick cycle 12 — env created, shapes verified)* — every `ManagedAgentsRuntime` method carries an unverified `VERIFY` comment (beta namespace, `anthropic-beta` header, tool `input_schema`). Check `client.beta.agents/vaults`, `MA_BETA_HEADER` (managed-agents-2026-04-01) against the current SDK/docs (use the claude-api skill) or every call 500s. _(P0 · blocked: Anthropic creds + current beta SDK/docs · `agents/runtime.py`, `worker/worker.py`, `shared/config.py`, `agents/tools/base.py` · done when: a smoke exercises create_environment/agent/session/send and no AttributeError/400 on shape)_ **PARTIAL 2026-06-09 (Nick): ALL shapes doc-verified via the claude-api skill — managed-agents-2026-04-01 header real; client.beta.{environments,agents,sessions,vaults} namespaces match runtime.py exactly (flat agents.create fields, top-level multiagent coordinator, bare self_hosted config, depth-1 limit confirmed). create_environment exercised LIVE: env_012JvqRKUZzUDeH3Gse6TBgZ created, id in uplift/env-id. agent/session/send live-smoke rides the roster provisioning (next AI-plane chunk). KEY FACT from docs: the ENVIRONMENT KEY is Console-generated (environment page → 'Generate environment key'), NOT returned by the API — uplift/env-key value parked on that user click.**
- [x] **Implement `create_environment`** *(code landed @30c7345, VERIFY-flagged; live smoke parked: needs MA creds)* — raises `NotImplementedError`; wire the self-hosted env create (first step of `coordinator.build`). _(P0 · blocked: Anthropic MA creds · `agents/runtime.py` · done when: returns a real env id; verify test passes)_
- [x] **Implement `create_agent` / `create_coordinator`** *(code landed @30c7345, VERIFY-flagged; live smoke parked: needs MA creds)* — both raise; wire specialist create + coordinator multiagent shape; serialize tools via `Tool.to_spec()`. _(P0 · blocked: Anthropic MA creds · `agents/runtime.py`, `agents/coordinator.py`, `agents/roster/__init__.py`, `agents/tools/base.py` · done when: `build()` creates env + 7 specialists + coordinator and returns a coordinator id)_
- [x] **Implement `create_session` + `send_message` (tool-use loop)** *(code landed @30c7345 incl. event-stream drain-to-idle; live smoke parked: needs MA creds)* — both raise; `send_message` must return the `{session_id, tenant_id, delegations, answer}` shape `FakeRuntime` returns plus pending approvals, carrying tenant_id+vault_id in metadata for worker RLS binding. _(P0 · blocked: Anthropic MA creds · `agents/runtime.py`, `conv/session.py` · done when: `Conversation.send` works end-to-end on `ManagedAgentsRuntime`)_
- [x] **(LANE NICK) Provision the org Anthropic API key into Secrets Manager (API-only)** — `uplift/anthropic-api-key` is empty; put the org key in, inject into the API service only, prove the worker never receives it. _(P0 · blocked: Anthropic creds + AWS apply (Nick) · `infra/modules/secrets/main.tf`, `infra/modules/api_service/main.tf`, `infra/modules/worker/main.tf`, `infra/main.tf` · done when: secret has the org key; API task has it, worker task provably does not)_ **DONE 2026-06-09 (Nick): org key in uplift/anthropic-api-key (user-supplied); api_anthropic_env flipped after uplift/env-id populated; live task-def rev 4 secrets = exactly [DB_USER, DB_PASS, ANTHROPIC_API_KEY, UPLIFT_ENV_ID]; worker module renders no org key (3-agent proof @#36). Zero-downtime roll, /healthz 200 ×3, /chat now 401-unauth (auth layer reached).**
- [x] **Wire a real `conversation_factory` (fixes `/chat` 503)** *(code @f15b70c + stub-id guard @#34; live 200 needs provisioned tenant row)* — `api/asgi.py` line 63 sets `conversation_factory=lambda tenant_id: None`. Build a `Conversation` with the managed runtime + per-tenant coordinator id + injected clients + real synthesizer; resolve the coordinator id from the tenant store (don't rebuild the roster per call). _(P0 · blocked: runtime methods + per-tenant coordinator store · `api/asgi.py`, `conv/session.py`, `api/app.py` · done when: `POST /api/chat` with a valid JWT returns a grounded answer (200))_
- [x] **Build a real tool executor (replace the noop)** *(code @f15b70c)* — `api/asgi.py` line 65 `executor=lambda action: {"status":"noop"}`; the gate calls this for AUTO + approved actions. Dispatch via `registry.resolve` with a tenant-bound `ToolContext`, or route to the worker queue. _(P0 · blocked: worker queue OR in-process tool clients · `api/asgi.py`, `api/control/gate.py`, `agents/tools/registry.py`, `worker/worker.py` · done when: an approved send_email sends, a read-only action returns real results, gate "executor never called on block/deny" invariant holds)_
- [ ] **(LANE NICK) Deploy the worker Fargate service + finish its env wiring** — module authored but unapplied; `worker.run()` reads `UPLIFT_ENV_ID` + `UPLIFT_ENV_KEY` but the task def only injects `UPLIFT_ENV_KEY` (and that secret is empty). Add `UPLIFT_ENV_ID`, db/cube/redis secrets, `CLOUDWATCH_METRICS=1`, apply. _(P0 · blocked: MA env created (creds) + AWS apply (Nick) · `infra/modules/worker/main.tf`, `worker/worker.py`, `infra/modules/secrets/main.tf`, `infra/main.tf` · done when: `uplift-worker` runs 1 task; `workers_polling` metric ≥1)_
- [x] **Persist per-tenant `coordinator_id` + `environment_id`** *(table #22, wiring #28)* *(tenant_workspaces table + WorkspaceStore landed @25c8c86; provisioning/asgi wiring = cycle 2)* — no column/store exists; `Conversation` needs coordinator_id, worker needs `UPLIFT_ENV_ID`. Rebuilding the roster per request (the `session.py` fallback) is wrong for prod. _(P0 · blocked: none (authorable); apply needs Nick · `db/schema.sql`, `signup/provisioning.py`, `conv/session.py`, `api/asgi.py` · done when: a tenant row stores both ids; factory + worker read them from the DB)_
- [ ] **(LANE NICK) Provision + rotate the MA environment key (`uplift/env-key`)** — secret container exists but empty; the **environment** key (not the org key) is produced by `create_environment` and authenticates the worker. Define create/store/rotate; keep the org key out. _(P0 · blocked: `create_environment` live + AWS apply · `infra/modules/secrets/main.tf`, `worker/worker.py`, `signup/provisioning.py` · done when: worker authenticates with the env key; org key provably absent; rotation documented)_
- [x] **Wire injected tool clients (db/cube/rag/cortex/greenlight) into live conversation + worker** *(#28 + #42 + cube client #47; live wiring of CUBEJS_API_SECRET_VALUE = Lane Nick)* — `ToolContext` + `_tool_ctx` clients are all `None` in prod. Supply a `crm_app` DB session with `set_tenant`, a tenant-scoped Cube client, a pgvector search client, the Cortex registry, the Greenlight queue. _(P0 · blocked: Cube deployed + DB connectivity + Cortex registry · `conv/session.py`, `api/asgi.py`, `worker/worker.py`, `agents/tools/base.py`, `agents/tools/readonly.py` · done when: search_rag/query_cube/read_crm/run_model return tenant-scoped data with `app.current_tenant` set)_

### P1
- [x] **Implement `create_vault` for per-conversation/per-user vaults** *(code landed @30c7345, VERIFY-flagged on name-vs-display_name)* — raises; vaults are the workspace-scoped isolation boundary `create_session(vault_id=...)` expects. _(P1 · blocked: Anthropic MA creds · `agents/runtime.py` · done when: returns a real vault id; two-user test confirms isolation)_
- [ ] **(LANE NICK) Add worker SG egress (443 to api.anthropic.com) + DB/Cube/Redis reachability** — worker uses `sg_api`; assert egress 443 (via NAT) and that `sg_api` permits worker→Aurora/Cube/Redis; existing SG drift already present. _(P1 · blocked: AWS apply (Nick) · `infra/modules/security/main.tf`, `infra/modules/worker/main.tf` · done when: worker can curl anthropic + connect to private endpoints; rule in TF state)_
- [x] **Provide a real RAG synthesizer (Anthropic model call) for `conv/rag.py`** *(#18, wired #28, ref-normalization #80)* *(AnthropicSynthesizer landed @30c27e3 as optional injection w/ enforced citation invariant; session/asgi wiring = cycle 2)* — `_default_synthesize` is trivially extractive; build a Sonnet-backed `Synthesizer` emitting `{claims:[{text, source_refs}]}` so the citation invariant holds. _(P1 · blocked: Anthropic creds · `conv/rag.py`, `conv/session.py`, `api/asgi.py` · done when: claims carry refs in the retrieved set; "no uncited claim" test still passes)_
- [x] **Replace `conv/session.py` regex action-routing with coordinator-driven tool selection** *(code @#42: regex gated to FakeRuntime)* — `_ACTION_TOOLS` regex is a flagged offline stand-in; the live coordinator should pick tools and route side-effecting ones to Greenlight. Gate the regex behind `FakeRuntime`. _(P1 · blocked: `send_message` tool-use loop · `conv/session.py`, `agents/runtime.py` · done when: an action utterance produces a pending approval because the coordinator chose the tool; FakeRuntime tests pass)_
- [x] **(LANE NICK) Deploy the Cube Fargate service** — module authored/unapplied; `query_cube`/`build_view` need Cube on Aurora (as `crm_app`, RLS) + Redis. Pin `cubejs/cube` digest, `CUBEJS_API_SECRET` from SM, internal-only. _(P1 · blocked: AWS apply (Nick) + pin Cube digest · `infra/modules/cube/main.tf`, `semantic/cube.js`, `semantic/model/cubes/`, `infra/main.tf` · done when: `uplift-cube` healthy on :4000 internally; `query_cube` returns governed metrics)_
- [x] **Wire the Cube tenant security context to the JWT claim** *(code @2b23f78: CubeClient mints per-request HS256 JWT from caller tenant; security.js hardened; live Cube verify = Lane Nick)* — `semantic/security.js` enforces a tenant filter but there's no client minting a per-request Cube JWT from the verified claim (THE TRUST RULE). _(P1 · blocked: Cube deployed + `CUBEJS_API_SECRET` · `semantic/security.js`, `agents/tools/readonly.py`, `agents/tools/build_view.py`, `api/asgi.py` · done when: query signs a Cube JWT from the claim; forged context rejected; no cross-tenant data)_
- [x] **Make `build_view`'s spec generator a real model call** *(code @#31 validate+retry; default-wired @#42)* — `build_view.py` requires `ctx.extra['generate_spec']` and raises if absent (faked in tests). Provide an Anthropic generator that validates against `view_spec.schema.json` (reject-and-retry). _(P1 · blocked: Anthropic creds + Cube members · `agents/tools/build_view.py`, `shared/view_spec.py`, `api/app.py`, `api/asgi.py` · done when: `build_view` returns `valid`; `/views/{id}/refine` produces a validated spec; invalid specs rejected)_
- [x] **Build the ingestion scheduler/infra (connectors → chunk → embed → pgvector)** *(code @e30c3b7: run_sync entrypoint + per-tenant SecretProvider + SET LOCAL cursors + Titan batch; REQ-004 filed — infra half = Lane Nick, now in PR #62)* — `ingest/` is authored+tested but has no scheduler module; RAG is empty until docs are populated. Add EventBridge→Fargate/Lambda running `sync_tenant` with Pg stores + real connector creds. _(P1 · blocked: AWS apply (Nick) + HubSpot creds · `ingest/pipeline.py`, `ingest/connectors/hubspot.py`, `infra/modules/secrets/main.tf`, `infra/main.tf` · done when: a scheduled sync populates pgvector; a 2nd run embeds ~0; search_rag returns hits)_
- [x] **End-to-end agent-plane verify script** *(scripts/verify_agent_plane.py @PR68, offline-PLAN mode default; LIVE run = Lane Nick, UPLIFT_LIVE_VERIFY gated)* — no live smoke exists. Provision a test tenant → chat (grounded answer + citations) → action (lands in Greenlight, no send) → approve (gated send runs) → exactly one trace. _(P1 · blocked: all P0 agent-plane items · `scripts/`, `conv/session.py`, `api/control/gate.py`, `worker/worker.py` · done when: the script passes against live creds and is gated out of offline CI)_

### P2
- [ ] **(LANE NICK) Deploy the Cortex retrain EventBridge target** — `modules/cortex` rule `uplift-cortex-retrain` has no target by design; add `aws_cloudwatch_event_target` → retrain compute (SageMaker/Lambda/Fargate `ml.retrain`) + EventBridge invoke/PassRole. _(P2 · blocked: AWS apply (Nick) + choose retrain compute · `infra/modules/cortex/main.tf`, `ml/retrain.py`, `infra/main.tf` · done when: `list-targets-by-rule` returns a target; a manual run registers a challenger)_
- [x] **Persist the Cortex model registry (replace `InMemoryRegistry`)** *(code @#32 S3+LocalFs; wired @#42)* — `ml/registry.py` is in-memory only; `run_model` loads `ctx.cortex.champion(tenant_id)` which needs a durable tenant-scoped (S3/Aurora) registry + (de)serialization. _(P2 · blocked: none (authorable); apply needs Nick · `ml/registry.py`, `ml/retrain.py`, `agents/tools/run_model.py` · done when: a champion promoted in one process loads in another; predict_proba reproducible)_
- [x] **Inject the Cortex registry into the tool context** *(code @#42: asgi+worker, CORTEX_* env names)* — `run_model` returns `score:None` because `ctx.cortex` is always None in prod. Inject the persistent registry into worker + conversation context. _(P2 · blocked: persistent registry + a trained champion · `agents/tools/run_model.py`, `worker/worker.py`, `conv/session.py`, `api/asgi.py` · done when: `run_model` returns a real score + model_version for a tenant with a champion)_
- [x] **Implement the Bedrock batch embeddings job for initial backfill** *(code @e30c3b7, VERIFY-flagged job shapes; live run = Lane Nick)* — `ingest/embed.py` `batch_embed` is a stub falling back to synchronous embed; implement the async Titan V2 batch (S3-JSONL → create job → poll → parse). _(P2 · blocked: AWS apply (Bedrock batch + S3) (Nick) · `ingest/embed.py`, `ingest/pipeline.py` · done when: a real model-invocation job runs to Completed and returns 1024-vectors parsed from S3)_
- [x] **Confirm hard MA limits are enforced by the live runtime** *(code-half: limits enforced + mock-tested @30c7345; LIVE confirmation vs the real MA API = Lane Nick w/ scripts/verify_agent_plane.py)* — `runtime.py` encodes depth=1, ≤20 agents, ≤25 threads but only `FakeRuntime` asserts the roster cap. Enforce against the live API. _(P2 · blocked: runtime methods · `agents/runtime.py`, `agents/coordinator.py` · done when: >20 agents / nesting / >25 threads is rejected/clamped with a clear error; tests assert each)_
- [x] **(LANE NICK) Restore ECR immutability + pin agent-plane image tags (worker/cube)** — worker defaults `uplift-worker:latest`, cube `cubejs/cube:latest`; build digest-pinned arm64 worker + pinned cube digest, flip ECR back to IMMUTABLE. _(P2 · blocked: AWS apply + image build (Nick) · `infra/modules/worker/main.tf`, `infra/modules/cube/main.tf`, `worker/Dockerfile`, `semantic/Dockerfile`, `infra/modules/ecr/main.tf` · done when: pinned digests in task defs; re-push of a tag rejected)_

### P3
- [x] **Implement the HIPAA Bedrock/1P fallback runtime behind the `runtime.py` seam** *(SelfHostedToolUseRuntime @PR68 — direct tool-use loop, same Greenlight routing; Bedrock-vs-1P endpoint choice VERIFY-flagged)* — `get_runtime` only knows `fake`/`managed`; add a third `AgentRuntime` impl so HIPAA tenants route off Managed Agents per the tenancy model. _(P3 · blocked: Bedrock access / 1P design · `agents/runtime.py`, `api/asgi.py`, `signup/provisioning.py` · done when: `get_runtime({'runtime':'bedrock'})` works unchanged; HIPAA sessions never touch MA)_

---

## Infrastructure & state

### P0
- [x] **Import the out-of-band ALB SG + 4 SG rules into state** — state (serial 71/6) has only api/db/redis SGs + 3 rules; the ALB SG and `alb_https_in`/`alb_http_from_cloudfront`/`api_from_alb`/`alb_egress` exist live but unmanaged. Next targeted apply would duplicate/error or drop ingress. _(P0 · blocked: none (needs AWS creds + live SG ids) · `infra/modules/security/main.tf`, `infra/main.tf` · done when: `plan` shows 0 changes for module.security; state lists the ALB SG + all rules)_
- [x] **Reconcile the live ALB/API/CloudFront stack into state** — state has ZERO resources for alb/api_service/api_cdn/cube/worker/observability/web_hosting; the live ALB, TG, listener, API task def+service, and CloudFront dist were created out-of-band. Import each or accept a one-time recreate. _(P0 · blocked: none (needs AWS creds + live ARNs) · `infra/modules/alb/main.tf`, `infra/modules/api_service/main.tf`, `infra/modules/api_cdn/main.tf`, `infra/main.tf` · done when: `plan` reports 0 add/0 destroy for alb/api_service/api_cdn against the live edge + running service)_ **DONE 2026-06-09 (Nick): premise was stale — state lists alb(3)/api_service(4)/api_cdn(3)/web_hosting(2); full plan @c5103b0 shows 0 add/0 change/0 destroy for all four. Evidence in `infra/RUNBOOK.md`.**
- [x] **Reconcile the full live stack + capture a clean baseline plan** — trunk wires modules not in live state (cube/worker/observability); a full `plan` post-deploy has not been re-verified. Triage every proposed change so trunk == reality. _(P0 · blocked: none · `infra/main.tf`, `infra/backend.hcl`, `infra/outputs.tf`, `infra/variables.tf` · done when: a reviewed `init+plan` has every line intentional, no surprise destroys; output attached to the runbook)_ **DONE 2026-06-09 (Nick): user restored both tfvars values → full plan is CLEAN (0 destroys; remaining adds = the intentionally-unapplied worker module + gated alarm). Evidence in RUNBOOK. Targeted-apply discipline stays as standard practice.**
- [x] **Raise Aurora backup retention + enable deletion protection / final snapshot / copy-tags** — live cluster has `backup_retention_period=1`, `deletion_protection=false`, `skip_final_snapshot=true`. Set retention 7–14, deletion protection on, final snapshot id, copy-tags, backup window. _(P0 · blocked: none (AWS creds) · `infra/modules/data/main.tf` · done when: `describe-db-clusters` shows retention≥7, deletion protection true, skip_final_snapshot false)_ **DONE 2026-06-09 (Nick): premise was mostly stale (retention=7 + protection already live); copy_tags applied via #23 targeted apply; live-verified copyTags=true. `infra/RUNBOOK.md`.**

### P1
- [x] **Import the 2 missing IAM inline secret policies** — `ecs_execution_secrets` + `api_task_secrets` are in code but not state (tasks pull DB_USER/DB_PASS live). Import both. _(P1 · blocked: none (AWS creds) · `infra/modules/iam/main.tf` · done when: `plan` shows 0 changes for module.iam; both policies in state)_
- [x] **Deploy the cube Fargate service** — module wired/validate-clean, zero state. Pin the `cubejs/cube` digest, confirm task-role secrets + SG ingress/egress, apply. _(P1 · blocked: none (AWS creds + cost; image digest) · `infra/modules/cube/main.tf`, `infra/main.tf` · done when: `describe-services uplift-cube` runningCount=1, reachable on :4000 internally)_ **DONE 2026-06-09 (Nick): /readyz 200 {"health":"HEALTH"} from an in-VPC probe after the SG self-rule (#58) + memory-driver fix (#59 — Cube 1.x removed redis). 1/1 steady. Follow-ups: bake semantic/ into a custom uplift-cube image (no data model in stock image); Cloud Map discovery. Previously: deployed @5ee986e — digest-pinned image, minted cube-api-secret, 1/1 STEADY STATE. :4000 probe exposed the missing sg_api self-rule (timeout) — rule in feat/nick-cube-sg, re-probe after apply. Also noted: no service discovery (Cloud Map) yet — api/worker can't resolve cube by name; CUBE_ENDPOINT stays "" until that lands (follow-up).**
- [ ] **Deploy the worker Fargate service** — wired/validate-clean, absent from state; authenticates with the env key (never org key). Set `var.image` to the real ECR URI, apply. Worker_absent alarm fires until this runs. _(P1 · blocked: Anthropic env key + AWS creds/cost · `infra/modules/worker/main.tf`, `infra/main.tf`, `agents/runtime.py` · done when: `describe-services uplift-worker` desired task running; `workers_polling` ≥1)_ **PARKED 2026-06-09 (Nick): needs Matt — same $200-ceiling cost call as cube; also still blocked on the MA env key (Anthropic creds).**
- [x] **Deploy the observability module (alarms + SNS)** — 6 alarms (alb_5xx, alb_latency p95, aurora_acu, redis_evictions, worker_absent) + SNS authored but zero in state; live backend has no operational alarms beyond billing. Set a real `notify_email`, apply. Worker_absent treats missing data as breaching → gate or deploy worker first. _(P1 · blocked: ALB-in-state reconcile + notify_email + AWS creds · `infra/modules/observability/main.tf`, `infra/main.tf`, `infra/envs/prod.tfvars` · done when: `describe-alarms` lists the 5 alarms with SNS actions; email subscription Confirmed)_ **DONE 2026-06-09 (Nick): 4 alarms + SNS live with actions; worker_absent correctly gated on var.worker_deployed (missing-data=breaching must not pre-date the worker); email subscription PendingConfirmation — user must click.**
- [x] **Deploy the provisioning Lambda + wire it to Step Functions** — the SFN machine exists but every Task points at a placeholder Lambda ARN (`provisioning_lambda_arn` empty). Package `signup/provisioning.Provisioner` as a Lambda, pass its ARN, scope `sfn_invoke` off wildcard, wire the Stripe webhook → StartExecution. _(P1 · blocked: provisioning client adapters + AWS creds/cost · `infra/modules/provisioning/main.tf`, `signup/provisioning.py`, `api/signup_routes.py` · done when: the SFN definition references the real ARN; a test StartExecution runs all steps → Activate)_
- [ ] **Acquire a domain + ACM cert, terminate TLS at the ALB, retire the api_cdn workaround** — no ACM/Route53 anywhere; HTTPS is faked via CloudFront default cert over plaintext ALB. The ALB module already does HTTPS:443 + redirect when `certificate_arn` is set. Buy domain, validate ACM (DNS), pass the cert, drop `http_forward` + `module.api_cdn`, re-point Amplify `/api`. _(P1 · blocked: domain purchase + Route53 zone · `infra/modules/alb/main.tf`, `infra/modules/api_cdn/main.tf`, `infra/main.tf`, `infra/variables.tf` · done when: ALB serves HTTPS on a real domain with ACM; HTTP only redirects; api_cdn removed from code+state; `curl https://<domain>/api/healthz` 200)_ **IN PROGRESS 2026-06-09 (Nick): friesenlabs.com bought (Squarespace); Route53 zone + wildcard ACM cert applied (PENDING_VALIDATION until the user's NS cutover — 4 NS handed over). ALB TLS cutover sequence to be authored in RUNBOOK before execution (listener swap + api_cdn origin + Amplify re-point).**
- [x] **Restore ECR `uplift-api` to IMMUTABLE** — flipped to MUTABLE during deploy (drift + supply-chain regression). Must move services off `:latest` first (IMMUTABLE forbids re-pushing `:latest`). _(P1 · blocked: depends on pinning services to digest tags · `infra/modules/ecr/main.tf` · done when: `describe-repositories` shows IMMUTABLE for all 3 repos; module.ecr plan clean)_
- [x] **Pin API/cube/worker images to digests/version tags (not `:latest`)** — `api_service`, cube, worker all default to `:latest` (no rollback, conflicts with IMMUTABLE). Add image vars carrying `@sha256` or semver, set per env. Cube has no var (hardcoded `:latest`). _(P1 · blocked: none (needs deployed digests) · `infra/modules/api_service/main.tf`, `infra/modules/cube/main.tf`, `infra/modules/worker/main.tf`, `infra/variables.tf`, `infra/envs/prod.tfvars` · done when: task defs reference `@sha256`; a re-apply of the same tfvars shows no image diff)_
- [ ] **Scale the API service to 2 tasks (HA across both AZs)** — `api_desired_count` default=2 but the live service runs 1; an AZ/task failure takes the API down. VPC spans 2 AZs. _(P1 · blocked: none (AWS creds + ~2x API cost) · `infra/modules/api_service/main.tf`, `infra/variables.tf` · done when: `describe-services` shows desired=2/running=2 across two AZs, both targets healthy)_ **PARKED (Nick): ~2× API cost vs the $200 ceiling; flip api_desired_count when the ceiling moves.**
- [x] **Commit `infra/.terraform.lock.hcl` with multi-platform hashes** — the lock is git-ignored (`.gitignore` line 39) so CI re-resolves providers; current lock only has darwin_arm64 hashes (fails on Linux CI). Run `terraform providers lock -platform=linux_amd64 -platform=darwin_arm64`, un-ignore, commit. _(P1 · blocked: none · `infra/.terraform.lock.hcl`, `.gitignore` · done when: the lock is tracked with both platforms' hashes; `init` succeeds on a clean Linux checkout)_ **DONE 2026-06-09 (Nick): lock tracked with 2×h1 per provider (aws + new random); this PR's green Linux CI init is the proof. Note `.gitignore` un-ignores it now.**
- [x] **Raise Aurora backup retention + enable Performance Insights (security-ops view)** — same cluster also lacks Performance Insights and IAM auth; enable PI for query-level visibility alongside the retention/protection change above. _(P1 · blocked: none · `infra/modules/data/main.tf` · done when: `describe-db-clusters` shows PerformanceInsightsEnabled=true on the instance)_ **DONE 2026-06-09 (Nick): applied via #23; live-verified PerformanceInsightsEnabled=true.**

### P2
- [ ] **Add Application Auto Scaling for API (and cube/worker)** — no `appautoscaling_target/policy` anywhere; every service is fixed-count. Add min/max + target-tracking on CPU/ALBRequestCountPerTarget for the API; worker should track queue depth. _(P2 · blocked: API at 2 tasks + observability metrics · `infra/modules/api_service/main.tf`, `infra/modules/cube/main.tf`, `infra/modules/worker/main.tf` · done when: `describe-scalable-targets` lists the API target; a synthetic load scales out then in)_
- [x] **Add ECS deployment circuit breaker + min/max healthy percent** — no service sets `deployment_circuit_breaker` or healthy percents; a bad image can drain to 0 with no rollback. Add `circuit_breaker{enable,rollback}` + 100/200 to each service. _(P2 · blocked: none · `infra/modules/api_service/main.tf`, `infra/modules/cube/main.tf`, `infra/modules/worker/main.tf` · done when: `describe-services` shows circuit breaker enabled/rollback; a broken task def auto-rolls-back)_
- [ ] **Make Redis HA (replica + automatic failover / multi-AZ) and consider AUTH** — live replication group is `num_cache_clusters=1`, no failover/multi-AZ; transit encryption on but no AUTH token. Bump to 2 nodes, enable failover + multi-AZ, add an auth token from SM (AUTH forces replacement). _(P2 · blocked: none (AWS creds + ~2x Redis cost) · `infra/modules/redis/main.tf` · done when: `describe-replication-groups` shows failover+multiAZ enabled, 2 members; clients use the AUTH token)_ **PARKED (Nick): ~2× Redis cost vs the $200 ceiling.**
- [x] **Pin the AWS provider to a bounded version** — `versions.tf` constrains `>= 5.40` while the lock resolved 6.49.0; an open lower bound lets `init -upgrade` jump majors. Tighten to `~> 6.49`. _(P2 · blocked: commit the lock file · `infra/versions.tf`, `infra/modules/cortex/.terraform.lock.hcl` · done when: `versions.tf` pins to 6.49.x; CI selects exactly that)_
- [ ] **Trim NAT egress with interface VPC endpoints** — single NAT (~$32/mo + per-GB); only the S3 gateway endpoint exists. Add interface endpoints for ecr.api/ecr.dkr/logs/secretsmanager/bedrock-runtime so only true internet egress uses NAT. _(P2 · blocked: none (AWS creds) · `infra/modules/vpc/main.tf`, `infra/modules/security/main.tf` · done when: `describe-vpc-endpoints` lists the interface endpoints in both subnets; NAT data-processing drops)_ **PARKED (Nick): ~$35/mo additive at current low egress — revisit when NAT data costs justify.**
- [ ] **Set `api_desired_count` explicitly in prod.tfvars** — default=2 but live runs 1 (unmanaged); after importing api_service the first plan wants 1→2. Decide intentionally and document the count in tfvars (don't rely on the default). _(P2 · blocked: depends on importing api_service · `infra/variables.tf`, `infra/envs/prod.tfvars`, `infra/modules/api_service/main.tf` · done when: tfvars sets the count; plan shows the intended count with no surprise diff)_

### P3
- [x] **Land org-baseline guardrails (Config recorder + delivery channel; SCP)** — account baseline (CloudTrail + S3 PAB) is in state, but there's no Config recorder/delivery channel and no SCP preventing CloudTrail/Config disablement. Add the recorder/channel (account-level now); SCP needs an Org. _(P3 · blocked: AWS Org context for the SCP · `infra/modules/baseline/main.tf` · done when: `describe-configuration-recorder-status` recording; delivery channel writes to S3)_
- [x] **Persist the out-of-band live endpoints as TF outputs / SSM params** — the live CloudFront/Amplify/ALB endpoints were set by hand; outputs.tf surfaces them only via modules not in state. After reconcile, surface them in outputs + mirror to SSM for deterministic web builds. _(P3 · blocked: depends on reconciling alb/api_cdn/web_hosting · `infra/outputs.tf`, `infra/modules/api_cdn/main.tf`, `infra/modules/web_hosting/main.tf` · done when: `terraform output` returns the live endpoints, mirrored to SSM)_ **DONE 2026-06-09 (Nick): /uplift/live/* SSM params + outputs verified.**

---

## Integrations & provisioning

### P0
- [x] **Implement a real Stripe adapter (customers, checkout, webhook signature)** *(code @ff157ea; live webhook secret wiring = REQ-003)* — `prod_deps._StubStripe` raises `NotImplementedError`; `payment.py` needs `create_customer`/`create_checkout_session`/`construct_event`. Build a thin adapter over `stripe`, map plan ids (starter/team/scale) to Price IDs, use `Webhook.construct_event`. The webhook is the only provisioning trigger. _(P0 · blocked: Stripe account + keys + Price IDs (Nick) · `api/prod_deps.py`, `signup/payment.py`, `requirements-api.txt` · done when: a `StripeAdapter` exists, `stripe` in requirements, a test exercises construct_event success+failure)_
- [x] **(LANE NICK) Wire `STRIPE_WEBHOOK_SECRET` + Stripe key as task secrets** *(pure task-def wiring — REQ-003 carries the full spec)* — `api/asgi.py` reads `STRIPE_WEBHOOK_SECRET` from env (always empty today → every webhook fails signature). Add the SM secret + the api task-def `secrets` block + execution-role GetSecretValue. _(P0 · blocked: Stripe account (Nick) + apply · `infra/modules/secrets/main.tf`, `infra/modules/api_service/main.tf`, `infra/modules/iam/main.tf`, `api/asgi.py` · done when: the deployed task has the Stripe env from SM)_
- [x] **Replace the in-memory `_AccountStore` with an Aurora-backed, RLS-correct store** *(code @55ab769 + #34: PgAccountStore/ledger/OTP behind SIGNUP_REAL_DEPS; REQ-002 grants = Lane Nick)* — `prod_deps._AccountStore` is a per-task dict lost on restart and not shared across the 2 tasks; no `accounts` table in schema. Add the table + a `PgAccountStore` (as `crm_app`) and decide its RLS story (pre-tenant rows have no tenant_id). _(P0 · blocked: none (code); schema apply needs Nick · `api/prod_deps.py`, `db/schema.sql`, `signup/accounts.py` · done when: a 2-task webhook (account created on one connection, webhook on another) provisions correctly)_
- [x] **Implement real email verification token issue + verify** *(code @#34: signed tokens + OTP, fake-clock proven; live send needs Resend domain = Lane Nick)* — `prod_deps` hardcodes `email_token_ok=False` (and `sms_code_ok=False`), so no account can ever verify and VERIFY-BEFORE-PAY blocks all checkouts. Issue a signed single-use 15-min token at create, send via Resend, validate it. Mirror for SMS OTP. _(P0 · blocked: Resend + SNS/Twilio (Nick) · `api/prod_deps.py`, `signup/accounts.py`, `api/signup_routes.py` · done when: a fake-clock test drives create→token→verify-email→verify-phone→may_pay True)_
- [x] **Build a real Resend email client (verification + welcome)** *(code @0f6c423, draft-gated by ALLOW_REAL_SENDS)* — no Resend code exists; `accounts.email.send_verification` + `resend.send_welcome` resolve to `_Noop`. Implement a `ResendEmailSender` over the Resend HTTP API, key from SM, templated HTML. _(P0 · blocked: Resend key + verified domain (Nick) · `api/prod_deps.py`, `signup/accounts.py`, `signup/provisioning.py`, `requirements-api.txt` · done when: send_verification/send_welcome call Resend (mocked in tests); wired from `RESEND_API_KEY`)_
- [x] **Implement the Anthropic Admin API client (workspace + scoped key + limits)** *(code @0f6c423, VERIFY-flagged endpoints; live = needs admin key)* — `provisioning.py` step 2 calls `admin.ensure_workspace/create_workspace_key/set_limits/delete_workspace`, all `_Noop`. One workspace per tenant is the isolation boundary. Verify endpoints against current Admin API docs. _(P0 · blocked: Anthropic Admin/Org key + endpoint verify (Nick) · `api/prod_deps.py`, `signup/provisioning.py`, `agents/runtime.py` · done when: a sandbox-org test creates + tears down a workspace idempotently)_
- [ ] **Implement the `agent_plane.ensure()` provisioning hook** *(PARKED-with-reason: the seam + stub-id guard exist (#28/#34); the real impl eagerly creates env+7 agents+coordinator per tenant at signup — a cost/architecture call (eager vs lazy-on-first-chat) for Nick+Matt to decide before wiring runtime.create_* into the Provisioner. ~1h of glue once decided.)* — step 3 calls `agent_plane.ensure(tenant_id, workspace_id)` but it's `_Noop` and the runtime create methods raise; a provisioned tenant has a workspace but no agents. Build a provisioner that creates env + 7 specialists + coordinator with the workspace key and stores the ids (idempotent). _(P0 · blocked: MA creds + beta verify (Nick) · `agents/runtime.py`, `agents/roster/__init__.py`, `agents/coordinator.py`, `signup/provisioning.py`, `api/prod_deps.py` · done when: ensure() creates env+roster+coordinator idempotently and records ids; a 2nd call is a no-op)_
- [x] **Implement real Cognito admin ops (create unconfirmed user, set `custom:tenant_id`, confirm)** *(code @ff157ea + #34 confirm() tightening; IAM grant = REQ-003/Lane Nick)* — `prod_deps._StubCognito` no-ops these, so a real signup's JWT would never carry `custom:tenant_id` → breaks THE TRUST RULE. Implement a boto3 `CognitoAdminClient` (admin_create_user suppressed/unconfirmed, set the immutable claim, admin_confirm_sign_up). _(P0 · blocked: live pool exists; needs IAM perms + wiring (Nick) · `api/prod_deps.py`, `signup/accounts.py`, `signup/provisioning.py`, `infra/modules/iam/main.tf` · done when: a provisioned user's issued JWT carries the correct `custom:tenant_id`)_
- [x] **Make the Greenlight side-effecting executor real** *(code @f15b70c: registry dispatch, tenant-bound, never-on-deny proven; live sends stay draft-gated)* — `api/asgi.py` `executor` is a noop; on approval nothing performs the side effect. Dispatch by action name (send_email→Resend, update_deal→CRM write, issue_quote→quote gen), read tenant from context (never body), RLS, idempotent per approval id, write a trace. _(P0 · blocked: Resend client + CRM write target · `api/asgi.py`, `api/control/gate.py`, `agents/tools/sideeffecting.py`, `api/control/greenlight.py` · done when: approving send_email sends, re-approving the same id does not re-send, a test proves exactly-once + a trace)_

### P1
- [ ] **(LANE NICK) Provision + verify the Resend sending domain (SPF/DKIM/DMARC)** — outbound verification/welcome require a verified domain or mail lands in spam; no domain owned yet. _(P1 · blocked: domain purchase + Resend account (Nick) · `infra/`, `signup/README.md` · done when: the domain shows verified with SPF/DKIM/DMARC green; a test email inbox-delivers)_
- [x] **Package + deploy the provisioning Lambda the SFN invokes** *(handler @80791c7: signup/lambda_handler.py idempotent per-step; REQ-005 filed — packaging/deploy = Lane Nick)* — the state machine's Task states point at a placeholder ARN; no handler code wraps `Provisioner` to run one idempotent step per Task with real injected clients. Write the handler, package it, add `aws_lambda_function`, pass the ARN. _(P1 · blocked: all provisioning adapters + apply · `infra/modules/provisioning/main.tf`, `signup/provisioning.py`, `api/prod_deps.py` · done when: an execution drives a verified account PAID→ACTIVE (or parks on injected failure), proven by history)_
- [x] **Connect the Stripe webhook to start the SFN execution (decouple from the request)** *(code @80791c7: SfnProvisioningTrigger, deterministic name, claim-ordered; live SFN = Lane Nick)* — today `handle_webhook` calls `provisioner.provision` synchronously inside the FastAPI request; real provisioning is slow and Stripe retries → duplicate work. Start an SFN execution keyed by account_id (deterministic name), return 200 immediately. Keep the in-process path for offline tests. _(P1 · blocked: provisioning Lambda + machine deployed; IAM StartExecution · `api/prod_deps.py`, `signup/payment.py`, `infra/modules/iam/main.tf` · done when: the webhook returns <1s and starts exactly one execution per account; re-delivery is a no-op)_
- [x] **Persist webhook/provisioning idempotency across restarts** *(stripe_events ledger @55ab769 + atomic claim @#34)* — idempotency relies on the in-memory store; with 2 tasks a re-delivered event hitting a fresh task re-provisions. Add a `stripe_events` ledger keyed by event id. _(P1 · blocked: Aurora-backed account store · `signup/payment.py`, `db/schema.sql`, `api/prod_deps.py` · done when: a replayed event id short-circuits before any state change across two store instances)_
- [x] **Per-tenant connector credential capture + storage in Secrets Manager** *(code @e30c3b7: SecretProvider resolves uplift/{tenant}/hubspot; IAM = REQ-004/Lane Nick)* — `uplift/connector-secrets` is a single empty container; `hubspot.py` resolves a single shared `HUBSPOT_TOKEN_SECRET_REF`, not `uplift/{tenant_id}/hubspot`, and there's no real boto3 `SecretProvider`. Define the per-tenant naming, a real provider, and an API to store a tenant's token. _(P1 · blocked: none (code); IAM secret perms + apply (Nick) · `infra/modules/secrets/main.tf`, `ingest/connectors/base.py`, `ingest/connectors/hubspot.py`, `infra/modules/iam/main.tf` · done when: HubSpotConnector resolves a real per-tenant token (mocked SM))_
- [x] **Implement real SMS OTP delivery (SNS or Twilio)** *(code @0f6c423: SnsSmsOtpSender, draft-gated, rate-limited OTP in #34)* — `accounts.sms` is `_Noop` and `sms_code_ok` is hardcoded False; the SignupFlow has a phone-verify step with no backing. Implement an `SmsSender` + OTP generate/validate (rate-limited, expiring, single-use). _(P1 · blocked: SNS SMS limit / Twilio (Nick) · `api/prod_deps.py`, `signup/accounts.py` · done when: a test drives phone-verify to phone_verified with expiry+single-use+rate-limit)_

### P2
- [x] **Build the real integrations/connect UI + backend** *(api half @3e555aa+hotfix #73 w/ shared-fallback guard; web panel @904259c; live secret-writes = REQ-006/Lane Nick)* — `web/src/screens/integrations.tsx` is a `@ts-nocheck` prototype faking connect with a setTimeout over `window.FL_DATA`. Add control-plane endpoints (list connectors, start OAuth, store creds, kick off `sync_tenant`) and wire the screen to the typed ApiClient. _(P2 · blocked: per-tenant secret store + HubSpot/Stripe OAuth apps (Nick) · `web/src/screens/integrations.tsx`, `web/src/api/client.ts`, `api/app.py`, `ingest/pipeline.py` · done when: Connect runs a real OAuth/token flow, persists, and a sync ingests rows)_
- [x] **Add webhook idempotency + rollback for the orphaned-charge risk** *(atomic claim #34, refund callback + retry entrypoint @80791c7, retry route @PR70)* — `provisioning.py` rollback only deletes the Anthropic workspace; a failure after cognito/tenant-context leaves the claim + tenant row in place with no retry sweeper and no refund path if provisioning ultimately fails after charge. Add a retry endpoint (provision is idempotent) + a terminal-failure Stripe cancel/refund. _(P2 · blocked: Stripe client (refund/cancel) + provisioning clients · `signup/provisioning.py`, `signup/payment.py`, `api/signup_routes.py` · done when: a PROVISIONING_FAILED account retries to ACTIVE; a forced terminal failure cancels/refunds the subscription)_
- [x] **Provisioning tenant-context correctness (Cube + db defaults)** *(tenant_settings table + PgTenantDefaults @PR70; cube step documented-as-noop — JWT is per-request)* — step 5 calls `cube.ensure_tenant_context` + `db.set_tenant_defaults` (both `_Noop`); nothing seeds a tenant's autonomy/cost-tag defaults. Implement `set_tenant_defaults`; implement-or-document-as-noop the Cube step. _(P2 · blocked: none (code); needs live DB schema · `signup/provisioning.py`, `api/prod_deps.py`, `semantic/security.js`, `db/schema.sql` · done when: a provisioned tenant has autonomy + cost-tag default rows; the Cube step is implemented or justified-as-noop)_
- [x] **Add live signup→pay→provision e2e tests (real Stripe test mode)** *(gated module @d5f64ea, skips cleanly w/o STRIPE_TEST_*; CI-job wiring = REQ-007/Lane Nick)* — all current signup tests use in-process fakes (`sig=='good'`, `_Noop`); none exercise a real signed webhook + real provisioning clients. Add a gated e2e (skipped without `STRIPE_TEST` creds) driving signup→checkout→signed-webhook→ACTIVE against sandbox providers. _(P2 · blocked: Stripe test keys + Anthropic sandbox + Cognito (Nick) · `tests/integration/test_api_signup.py`, `scripts/smoke/`, `.github/workflows/ci.yml` · done when: the gated e2e passes when creds present, skips cleanly otherwise)_

### P3
- [x] **Wire the server-side PostHog funnel client** *(@PR70, gated under SIGNUP_REAL_DEPS + POSTHOG_PROJECT_KEY_VALUE)* — `payment.py`/`provisioning.py` accept an injected PostHog client but `prod_deps` constructs them without one (funnel defaults to None), so no server-side revenue/provisioning analytics in prod. Implement a real client, read key from config, inject. _(P3 · blocked: PostHog project key (Nick) · `api/prod_deps.py`, `signup/funnel.py`, `web/src/analytics/posthog.ts` · done when: a staging payment produces payment_succeeded + instance_provisioned grouped under the tenant)_

---

## Security / Ops / Cost

### P0
- [x] **Close the ALB origin with a CloudFront shared-secret header** — the internet-facing ALB's only ingress is port 80 from the shared CloudFront prefix list (`pl-3b927c52`), and CloudFront sends no custom origin header, so any attacker's CloudFront distro reaches `/api` over plaintext, bypassing the edge. Add an `X-Origin-Verify` secret injected by api_cdn custom_headers, require it at the ALB listener rule or in FastAPI (403 if absent), rotate via SM. _(P0 · blocked: none · `infra/modules/api_cdn/main.tf`, `infra/modules/alb/main.tf`, `infra/modules/security/main.tf`, `api/app.py`, `infra/modules/secrets/main.tf` · done when: direct `curl http://<alb>/api/healthz` → 403, while `curl https://d1vw20lc...cloudfront.net/api/healthz` → 200)_ **DONE 2026-06-09 (Nick) — APPLIED + LIVE-VERIFIED: phase 1 (secret uplift/origin-verify + CloudFront header, distro ETZLYZ2VC4KBI Deployed) then phase 2 (listener default→fixed-response 403 + priority-10 X-Origin-Verify forward rule). Edge /healthz 200 ×3 after enforcement; describe-rules confirms 403-default + header rule. Direct-ALB 403 curl is untestable from outside (SG admits only the CloudFront prefix list — strictly tighter than the done-when assumed); config-level proof in `infra/RUNBOOK.md`. Previously: authored infra-only (SM secret + random value, CloudFront custom_header, ALB 403-default + header-match rule) behind two flags — `enable_origin_verify` (phase 1: header stamping) then `alb_enforce_origin_verify` (phase 2: enforcement) — so there is no 403 window. Phased targeted applies follow merge; procedure in `infra/RUNBOOK.md`.**
- [x] **Run `scripts/isolation_test.py` against LIVE Aurora as `crm_app`** — the isolation gate has only ever run against an ephemeral CI container, never the live FORCE'd-RLS cluster. Run it via a one-off Fargate task using the `crm_app` DSN (NOT the owner — RLS is bypassed by owners); confirm `crm_app` is NOSUPERUSER/NOBYPASSRLS. _(P0 · blocked: none · `scripts/isolation_test.py`, `db/roles.sql`, `db/schema.sql`, `infra/modules/api_service/main.tf` · done when: the task prints "[isolation] PASS — RLS enforced" and `\du crm_app` shows no Superuser/Bypass RLS)_ **DONE 2026-06-09 (Nick): one-off Fargate task f3ed4a5c… (image dc7a352, crm_app DSN) printed exactly '[isolation] PASS — RLS enforced; no cross-tenant read/write.'; live pg_roles: rolsuper=f, rolbypassrls=f. `infra/RUNBOOK.md`.**
- [x] **Import the CLI-added SG rules into state (security view)** — same drift as the infra reconcile item: `alb_http_from_cloudfront`, `api_from_alb`, ALB egress exist live but aren't in state; a `-target=module.security` apply could drop ingress and take the API offline. _(P0 · blocked: none · `infra/modules/security/main.tf`, `infra/main.tf` · done when: `plan -target=module.security` shows 0 add/change/destroy for those rules; state lists 6+ rule resources)_
- [x] **Reconcile the full live stack + a clean baseline plan (security view)** — trunk wires cube/worker/observability not in live state; a clean full `plan` against the live account hasn't been re-verified post-deploy. Triage every line before any change. _(P0 · blocked: none · `infra/main.tf`, `infra/backend.hcl`, `infra/outputs.tf`, `infra/variables.tf` · done when: `init+plan` is reviewed with no surprise destroys of live ALB/api_service/Aurora; output in the runbook)_ **DONE 2026-06-09 (Nick): clean plan achieved — see the twin item.**

### P1
- [x] **Put a WAFv2 web ACL on the CloudFront API edge** — the distribution has `WebACLId=""`; the public multi-tenant API has no rate limiting, managed rules, or per-IP throttling. Add `CommonRuleSet` + `KnownBadInputs` + a rate-based rule, associate it. _(P1 · blocked: none · `infra/modules/api_cdn/main.tf`, `infra/main.tf` · done when: `get-distribution` shows a non-empty WebACLId; a burst trips the rate rule)_
- [x] **Deploy the observability module (security view)** — none of the 6 alarms or alerting SNS are live; the system has no alerting on 5xx/latency/Aurora/Redis. Apply with a real `notify_email`; gate worker_absent (breaching-on-missing) since the worker isn't deployed. _(P1 · blocked: none · `infra/modules/observability/main.tf`, `infra/main.tf`, `infra/variables.tf`, `infra/envs/prod.tfvars` · done when: `describe-alarms` lists the alarms with SNS actions; email subscription Confirmed)_ **DONE 2026-06-09 (Nick): 4 alarms + SNS live with actions; worker_absent correctly gated on var.worker_deployed (missing-data=breaching must not pre-date the worker); email subscription PendingConfirmation — user must click.**
- [x] **Fix the billing alarm/budget notification + the $200 vs $500 mismatch** — the live `uplift-monthly` budget is $500 with an empty subscriber list, and the `uplift-billing` alarm has no action; nobody is paged. Set `notify_email`, set the budget to the agreed figure, give the alarm an SNS action. _(P1 · blocked: none · `infra/modules/guardrails/main.tf`, `infra/variables.tf`, `infra/envs/prod.tfvars` · done when: `describe-notifications-for-budget` returns a confirmed subscriber; the alarm has a non-empty AlarmActions)_ **DONE 2026-06-09 (Nick): notification live with theogyeezy@gmail.com subscribed (verified; created via direct CLI after a terraform/Budgets consistency quirk); uplift-billing AlarmActions = the alarms SNS topic. Limit left at $500 deliberately — flag if $200 is wanted.**
- [x] **Build a real CI/CD deploy pipeline** — `ci.yml` only tests/validates; no job builds+pushes the arm64 image, runs `terraform apply`, or triggers Amplify; the manual deploy is how the drift happened. Add an OIDC-assumed deploy workflow (buildx arm64 → ECR immutable tag, plan artifact + manual-approval apply, force-new-deployment, Amplify trigger). _(P1 · blocked: none · `.github/workflows/ci.yml`, `.github/workflows/deploy.yml`, `api/Dockerfile`, `infra/main.tf`, `infra/modules/api_service/main.tf` · done when: a tagged push builds+pushes `uplift-api:<sha>`, applies with approval, rolls the service; `/api/healthz` 200 on the new task)_ **DONE 2026-06-10 (Nick): FULL PIPELINE PROVEN END-TO-END — build (OIDC→buildx arm64→ECR) → plan (artifact, verified no stateful destroys) → approved apply → service roll → edge health-gate, all green (run 27262716270). Prod API rolled from the weeks-old e0794bc to current main `uplift-api:14524b0` (rev 6, 1/1, /healthz 200). The deploy is now the standard release path.**
- [x] **Raise Aurora backup retention + deletion protection + final snapshot + PI** — (same as the infra item; tracked here for the data-loss risk). _(P1 · blocked: none · `infra/modules/data/main.tf` · done when: retention≥7, deletion protection true, skip_final_snapshot false, PI enabled)_ **DONE 2026-06-09 (Nick): all four conditions live-verified after #23 apply.**

### P2
- [ ] **Enable the Budget Deny-at-90% action (hard cost stop)** — the `aws_budgets_budget_action` (attaches AWSDenyAll at 90%) is count-gated on an empty execution-role arn, so it's not created; the budget is advisory only. Create the Budgets execution role + set deny targets, apply. _(P2 · blocked: none · `infra/modules/guardrails/main.tf`, `infra/modules/iam/main.tf`, `infra/variables.tf` · done when: `describe-budget-actions-for-budget` returns the APPLY_IAM_POLICY action at 90% with a valid role)_
- [x] **Add a CloudWatch dashboard for the live stack** — 0 dashboards exist; Container Insights is on but unsurfaced. Add a dashboard (ALB req/5xx/latency, ECS task/CPU/mem, Aurora ACU/connections, Redis hit-rate, worker_absent). _(P2 · blocked: none · `infra/modules/observability/main.tf` · done when: `list-dashboards` shows a uplift dashboard rendering live widgets)_ **DONE 2026-06-09 (Nick): `uplift-live` applied from main @835d1c0; `list-dashboards` shows it; 6 widgets over the live ALB/ECS/Aurora/Redis identifiers.**
- [x] **Grant the api task role `xray:PutTraceSegments` (ADOT export)** — the api task runs an aws-otel-collector sidecar (essential=false) but the task role has no xray perms, so it fails silently; X-Ray tracing is authored but dead. Add PutTraceSegments/PutTelemetryRecords (+ ssm:GetParameters). _(P2 · blocked: none · `infra/modules/iam/main.tf`, `infra/modules/api_service/main.tf` · done when: the X-Ray service map shows uplift-api receiving segments)_ **PARTIAL 2026-06-09 (Nick): `xray-export` policy APPLIED @835d1c0 (get-role-policy verified) — but get-trace-summaries shows 0 segments under live traffic: the api app does not emit OTLP to the sidecar. Infra half done; remainder = app-side instrumentation (LANE MATT — wire OTLP export in the api to localhost:4317).**
- [ ] **Restore ECR `uplift-api` to IMMUTABLE + add a lifecycle policy** — repo is MUTABLE (others IMMUTABLE); move deploys to immutable `:<git-sha>`, flip back, add a lifecycle policy to expire untagged/old images. _(P2 · blocked: none (pair with CI/CD digest tags) · `infra/modules/ecr/main.tf` · done when: `describe-repositories uplift-api` IMMUTABLE + lifecycle policy attached; task def references a sha tag)_
- [x] **Establish secrets rotation for the static `uplift/*` secrets** — only the RDS master rotates; crm-app-db / anthropic-api-key / cube-api-secret / connector-secrets / env-key all have rotation=null. The static `crm_app` password is the credential for all tenant data under RLS. Add Lambda rotation for crm-app-db + a documented runbook for the third-party keys. _(P2 · blocked: none (Anthropic rotation needs creds) · `infra/modules/secrets/main.tf`, `infra/modules/data/main.tf`, `api/migrate.py` · done when: `describe-secret uplift/crm-app-db` shows RotationEnabled=true; a rotation succeeds with the API still serving)_ **DONE 2026-06-09 (Nick): SAR single-user rotation Lambda (VPC'd, 30-day, rotate_immediately=false) + one controlled rotation EXECUTED: rotated, api+cube rolled immediately per RUNBOOK, healthz 200×3 + authed 401 path verified. Third-party key cadence documented in RUNBOOK.**
- [x] **Enable CloudTrail data events (S3 + Secrets) + ALB access logging** — CloudTrail logs management events only; object-level S3 reads and ALB request logs are unaudited (ALB `access_logs.s3.enabled=false`). Add S3 data-event selectors + ALB access logging to an encrypted bucket. _(P2 · blocked: none · `infra/modules/baseline/main.tf`, `infra/modules/alb/main.tf`, `infra/modules/s3/main.tf` · done when: `get-event-selectors` shows S3 data resources; ALB access logs enabled)_ **DONE 2026-06-09 (Nick): both halves live-verified — get-event-selectors shows [uplift-s3-objects, management-events] @6703381; ELBAccessLogTestFile delivered to uplift-alb-logs-186052668426. Item premise correction recorded: Secrets Manager has NO CloudTrail data events — GetSecretValue is a management event the trail already captures (PutEventSelectors rejects the type; verified live).**
- [x] **Tighten api-task secrets scope + scope the SFN `lambda:InvokeFunction` off wildcard** — the api task role can GetSecretValue on all `uplift/*` (reads keys it never uses); scope to crm-app-db (+rds! master). The provisioning SFN role falls back to `Resource="*"` when the Lambda ARN is empty — pin it before applying. _(P2 · blocked: none · `infra/modules/iam/main.tf`, `infra/modules/provisioning/main.tf` · done when: the api role lists only crm-app-db/master ARNs; module.provisioning plan has no `Resource="*"`)_ **DONE 2026-06-09 (Nick): APPLIED @c8f2f10 + live-verified — get-role-policy shows exactly the crm-app-db + rds!cluster master ARNs on the api role, and the placeholder-function ARN (no `*`) on the SFN role; edge 200 + unauth route unchanged after. Previously: authored — api task role narrows uplift/*+rds!* → the exact crm-app-db + rds!cluster master ARNs (grep proves migrate's boto3 reads are the only runtime surface); sfn_invoke fallback `*` → the existing placeholder-function ARN (inert grant). Targeted plan = exactly the 2 in-place policy changes; applies post-merge.**
- [ ] **Cost-trim levers + a real cost trajectory check** — CE shows $0 MTD (lags ~24h); the ~$6-7/day estimate isn't observable yet. Levers: VPC endpoints (replace NAT egress), confirm Aurora min-ACU floor, drop log retention if not needed, single-AZ NAT. Re-measure after 48h. _(P2 · blocked: none (24-48h for CE data) · `infra/modules/vpc/main.tf`, `infra/modules/data/main.tf`, `infra/modules/api_service/main.tf`, `shared/COST.md` · done when: a full-day `get-cost-and-usage` shows <$7/day; a 30-day projection recorded vs the $200 cap)_

### P3
- [ ] **Switch CloudFront→ALB origin off plaintext HTTP (TLS edge-to-origin)** — api_cdn uses `origin_protocol_policy=http-only` over port 80; the edge→ALB leg is unencrypted. Once a domain + ACM cert land on the ALB, set origin to https-only. _(P3 · blocked: domain purchase + ACM cert · `infra/modules/api_cdn/main.tf`, `infra/modules/alb/main.tf`, `infra/modules/security/main.tf` · done when: origin OriginProtocolPolicy=https-only; ALB has a 443 listener with a valid cert)_
- [ ] **Drop the ALB:80-from-CloudFront SG rule once HTTPS-on-ALB is live** — `alb_http_from_cloudfront` + the prefix-list data source exist only for the no-domain workaround; remove them in lockstep with retiring api_cdn so `alb_https_in` (443) is the sole public ingress. _(P3 · blocked: depends on real domain + ACM on the ALB · `infra/modules/security/main.tf` · done when: the ALB SG has only 443 ingress + egress; the http rule + prefix-list source are gone; plan clean)_
- [x] **Enable ECS Exec (break-glass debugging) on the api service** — `enableExecuteCommand=false`; tasks run in private subnets with no shell access for live incident debugging. Enable execute-command (+ ssmmessages on the task role), guarded to non-prod/approval. _(P3 · blocked: none · `infra/modules/api_service/main.tf`, `infra/modules/iam/main.tf` · done when: `ecs execute-command ... whoami` opens a session against a live task)_ **DONE-infra 2026-06-09 (Nick): enableExecuteCommand=true live (rev 5) + ssmmessages on the task role. The laptop's session-manager-plugin cask failed to install — client-side only; install when a shell is actually needed.**
- [x] **Reduce CloudWatch log retention drift + confirm KMS encryption** — the two live log groups are 30d with no KMS and no central retention policy for future services. Standardize retention (+ optional KMS) via a shared variable. _(P3 · blocked: none · `infra/modules/api_service/main.tf`, `infra/modules/observability/main.tf` · done when: all uplift log groups share one retention variable; `describe-log-groups` shows consistent retention/encryption)_ **DONE 2026-06-09 (Nick): one root `log_retention_days` knob (default 30) threaded through api_service/cube/worker (all 6 log groups); zero-diff at the default — live groups verified consistent at 30d. KMS deliberately not added (CMK on log groups = extra cost + key-policy complexity for a single-account stack; revisit with the HIPAA runtime).**
- [x] **Add GuardDuty + AWS Config + the SCP-deny follow-up** — no GuardDuty, no Config recorder for the account now holding tenant data behind an internet-facing ALB. Enable GuardDuty + Config baseline; the SCP needs an Org. _(P3 · blocked: AWS Org context for the SCP · `infra/modules/baseline/main.tf` · done when: `list-detectors` shows an enabled GuardDuty; `describe-configuration-recorders` shows a recorder; SCP tracked as an Org follow-up)_ **DONE 2026-06-09 (Nick): detector live; recorder recording=True into the trail bucket (config/). SCP remains the Org follow-up.**
---

## Security audit (2026-06-09, 37-agent adversarial)

27 confirmed findings. ✅ = fixed this session.

- [x] **[CRITICAL] Live API shares ONE psycopg2 connection across all tenants and sets app.current_tenant with session-level SET (not SET LOCAL) — concurrent requests can read/write across tenants** — _RLS correctness end-to-end_. Fix: Do not share one connection across tenants/threads. Use a connection pool and check out a fresh connection per request, or wrap each tenant operation in a transaction using `SET LOCAL app.current_tena
- [x] **[HIGH] api TASK role can read every uplift/* secret at runtime, not just its DB credential** — _IAM least-privilege (api task role)_. Fix: Scope the api task role to exactly the secret it needs at runtime: Resource = [crm_app_db_secret_arn]. Remove rds!* and the uplift/* wildcard from the TASK role (the master secret is only needed by th
- [x] **[HIGH] Aurora cluster has deletion protection OFF and only 1-day backup retention** — _Aurora durability/recoverability_. Fix: Set deletion_protection=true, raise backup_retention_period to a real RPO (e.g. 7-35 days), set skip_final_snapshot=false (or a deletion plan with a final snapshot), and add a second serverless instan
- [x] **[MEDIUM] Any CloudFront customer can reach the origin ALB: prefix-list ingress + NO shared-secret header + NO app-layer origin check** — _Network/edge — CloudFront-to-ALB origin trust_. Fix: Add a high-entropy secret custom header on the distribution origin (aws_cloudfront_distribution.origin.custom_header in infra/modules/api_cdn/main.tf, value from Secrets Manager) AND enforce it at the
- [x] **[MEDIUM] No WAF on the CloudFront distribution or the ALB** — _Network/edge — perimeter filtering_. Fix: Attach an AWS WAFv2 web ACL (scope CLOUDFRONT) to distribution ETZLYZ2VC4KBI with AWSManagedRulesCommonRuleSet + KnownBadInputs + a rate-based rule, and add web_acl_id to aws_cloudfront_distribution.a
- [ ] **[MEDIUM] CloudFront default certificate forces weak minimum TLS (TLSv1) at the viewer edge** — _Network/edge — viewer TLS_. Fix: Move to a custom domain + ACM cert on the distribution and set minimum_protocol_version = 'TLSv1.2_2021'. This is the same domain/cert work that also fixes the http-only origin hop (finding #2).
- [ ] **[MEDIUM] CloudFront-to-ALB origin leg is plaintext HTTP — the ID-token bearer crosses AWS's public network unencrypted** — _Transport / token confidentiality_. Fix: Add an HTTPS:443 listener to uplift-alb with an ACM cert and switch the CloudFront origin to `OriginProtocolPolicy: https-only` (or `match-viewer`), so the bearer token is encrypted end-to-end. The :8
- [ ] **[MEDIUM] Shared ecsTaskExecutionRole grants all three services (api, cube, worker) read on every uplift/* + rds!* secret** — _IAM least-privilege (execution role)_. Fix: Either (a) give each service its own execution role with a Resource list narrowed to the exact secret ARNs that service's task def injects (api => crm-app-db only), or (b) keep one execution role but 
- [ ] **[MEDIUM] Aurora does not enforce TLS (rds.force_ssl=0) and the app/migrate DSNs set no sslmode — crm_app credentials and tenant data can traverse the network in plaintext** — _Encryption in transit (Aurora)_. Fix: Set rds.force_ssl=1 on a CUSTOM cluster parameter group (the default group cannot be edited) and reboot, AND make the app fail closed in transit: append `?sslmode=verify-full&sslrootcert=<rds-combined
- [x] **[MEDIUM] API/worker images and the live task def pin floating :latest tags (non-reproducible, mutable supply chain)** — _API image contents / supply chain_. Fix: Pin base images by digest (FROM python:3.13-slim@sha256:..., cubejs/cube:vX.Y@sha256:...). Tag app images with immutable content tags (git SHA) and reference that tag in prod.auto.tfvars + the task de
- [ ] **[LOW] Plaintext edge→origin: CloudFront origins to the ALB over HTTP (http-only), so all API traffic crosses AWS network in cleartext** — _Network/edge — CloudFront origin protocol_. Fix: Provision an ACM cert + domain on the ALB and switch the ALB to the HTTPS:443 listener path already authored at infra/modules/alb/main.tf:40-52 (the has_cert branch), then set CloudFront origin_protoc
- [ ] **[LOW] ALB security group opens 443 to 0.0.0.0/0 with no 443 listener — misleading dead rule, and any future 443 listener is world-open** — _Network/edge — ALB security group hygiene_. Fix: Either remove the 443/0.0.0.0/0 rule (it serves no live listener) or, when HTTPS is enabled, restrict the 443 ingress to the CloudFront origin-facing prefix list (same as the 80 rule) so the ALB is ne
- [ ] **[LOW] ALB and API security-group egress is allow-all (0.0.0.0/0, all protocols)** — _Network/edge — egress controls_. Fix: Remove ALB egress entirely (or scope to the API SG on 8000). For the API tier, replace allow-all with the VPC-endpoint approach already noted as a cost lever in infra/modules/vpc/main.tf:60-62 (interf
- [x] **[LOW] CloudFront access logging disabled and no response-headers/HSTS policy** — _Network/edge — edge observability & response hardening_. Fix: Enable standard logging to an S3 bucket (logging_config) and attach a ResponseHeadersPolicy with Strict-Transport-Security and the standard security headers in infra/modules/api_cdn/main.tf.
- [x] **[LOW] Cognito Hosted UI public self-registration is open on a pool meant to be provisioning-only** — _Cognito pool config / account lifecycle_. Fix: Set AllowAdminCreateUserOnly=true (provisioning creates users via AdminCreateUser), and/or disable the Hosted UI sign-up page for the app client. Also turn MFA on (or enforce via the app) for the admi
- [x] **[LOW] Refresh token lives 30 days with no rotation; client deliberately keeps the same refresh token across refreshes** — _Token lifetime / revocation_. Fix: Shorten RefreshTokenValidity (e.g. 1-7 days) for a browser SPA, and consider httpOnly cookie storage for the refresh token if a session backend is added. The revoke-on-signout path is good; keep it.
- [x] **[LOW] User pool has DeletionProtection INACTIVE** — _Cognito pool resilience_. Fix: Set DeletionProtection: ACTIVE on the pool (and confirm the same on the Aurora cluster / critical resources).
- [ ] **[LOW] Demo-user login password sits in Secrets Manager under the broad uplift/* grant, with no CMK and no rotation** — _Secrets Manager (demo-user credential)_. Fix: Treat the demo login as a Cognito user managed via the user pool, not a long-lived Secrets Manager password; if it must persist as a secret, remove it from the wildcard grant (it is never injected int
- [ ] **[LOW] No rotation on any uplift/* secret (DB app creds, Anthropic key, connector creds, Cube signing secret)** — _Secrets Manager (rotation)_. Fix: Enable rotation (or a documented manual rotation cadence) for uplift/crm-app-db and uplift/cube-api-secret at minimum. crm-app can be rotated by an RDS-style rotation Lambda; cube-api-secret rotation 
- [x] **[LOW] provisioning Step Functions role can invoke ANY Lambda in the account (lambda:InvokeFunction on Resource *)** — _IAM least-privilege (provisioning-sfn)_. Fix: Scope Resource to the specific provisioning Lambda ARN(s) (arn:aws:lambda:us-east-1:186052668426:function:uplift-provisioning-*), not '*'.
- [ ] **[LOW] rds!* wildcard in the secret grant is broader than the single Aurora master it targets** — _IAM least-privilege (rds!* wildcard)_. Fix: Pin to the exact master secret ARN (or its prefix rds!cluster-a47e8ca1-18db-4a9b-9dbc-ae0a6a4ba80c-*) instead of rds!*, and only on the role/task that actually runs migrate, not the always-on api serv
- [ ] **[LOW] Redis (Valkey) has no AUTH token and no RBAC user group — tenant isolation relies entirely on app-side key prefixing** — _Redis auth_. Fix: Enable Valkey RBAC with a least-privilege user group (or at minimum an AUTH token stored in Secrets Manager) so cache access requires a credential, not just network reachability. Wire the credential i
- [x] **[LOW] crm_app database credential is not on rotation** — _Aurora credential hygiene_. Fix: Put uplift/crm-app-db on a rotation schedule using a Postgres single-user rotation Lambda that also issues ALTER ROLE crm_app PASSWORD against Aurora so the DB and secret stay in sync.
- [ ] **[LOW] Public GitHub repo leaks the live AWS account ID and live resource hostnames** — _Public-repo exposure_. Fix: Make the repo private, or scrub the literal account id + live hostnames from README/CLAUDE/BUILD_STATUS/TODO and replace with placeholders (e.g. <ACCOUNT_ID>). Treat account id + Cognito pool/client i
- [ ] **[LOW] Python runtime dependencies are entirely unpinned (>=) with no lockfile or hashes** — _Dependency pinning_. Fix: Compile pinned, hashed requirements (pip-compile --generate-hashes) and have the Dockerfile install with `pip install --require-hashes`. At minimum pin exact == versions for the image. Keep the >= dev
- [ ] **[LOW] GitHub Actions are pinned to mutable major-version tags, and the workflow declares no permissions block** — _CI supply chain / token scope_. Fix: Pin actions to full commit SHAs (e.g. actions/checkout@<40-char-sha>) with a comment of the version, and add an explicit top-level `permissions: { contents: read }` to the workflow.
- [x] **[LOW] .stignore drops the docs/*.pdf parity but not docs/*.txt (Syncthing leak of the confidential build guide)** — _Gitignored-files hygiene (sync side channel)_. Fix: Add `docs/*.txt` (or the specific docs/uplift-build-guide.txt) to .stignore to match .gitignore, so the confidential spec does not leave the machine via Syncthing.

## Cost trim levers (run-rate ~$185/mo, $200 is a TOTAL ceiling → ~33-day runway)

Prioritized by $/mo saved. SAFE-NOW = no functional impact at current pre-launch/idle state; WAIT-UNTIL-LAUNCH = could affect availability/perf once real traffic arrives.

- [ ] **Replace NAT Gateway with VPC interface endpoints (ECR api+dkr, Secrets Manager, CloudWatch Logs, STS) and delete NAT** — saves ~$33/mo (or net ~$11/mo if you keep ~3 single-AZ interface endpoints at ~$7.30/mo each; full removal is cheapest since the Fargate task only egresses to AWS services + S3 already has a free gateway endpoint). Effort: MEDIUM (Terraform: add endpoints, repoint private route table, verify image pulls still work). **SAFE-NOW** (do before traffic; validate a task can still pull its image and read secrets).
- [ ] **Lower Aurora MinCapacity from 1.0 to 0.5 ACU** — saves ~$43.80/mo (floor drops $87.60 → $43.80). Live data shows it pins at the floor with CPU ~2.8%, so 0.5 has ample headroom pre-launch. Effort: LOW (single SLv2 scaling-config change). **SAFE-NOW** for dev/pre-launch; re-raise the floor before production load if latency-sensitive.
- [ ] **Aurora auto-pause to 0 ACU when idle** (pg 16.x SLv2 supports min=0) — saves up to ~$87/mo, drops idle Aurora to near-$0 with a few-second cold-start resume. Effort: LOW-MEDIUM. **SAFE-NOW** for non-prod; **WAIT-UNTIL-LAUNCH** for prod (cold-start latency on first query).
- [ ] **Downsize ElastiCache cache.t4g.small → cache.t4g.micro** — saves ~$11.68/mo ($23.36 → $11.68). Mem usage is ~0.88%, fits easily in micro's ~0.5GB. Effort: LOW (node-type modify, brief blip). **SAFE-NOW**; revisit if working set grows past ~0.5GB. (Or pause Redis entirely if the app can run without cache pre-launch → -$23.36.)
- [ ] **Right-size Fargate task 512/1024 → 256/512** — saves ~$9/mo ($18 → ~$9). CPU avg 0.40%, mem ~70MB of 1024MB. Effort: LOW (new task-def revision). **WAIT-UNTIL-LAUNCH** only because the api + aws-otel-collector sidecar want a little headroom; 256/512 is fine now but verify the sidecar fits. SAFE-NOW if you drop/trim the otel sidecar.
- [ ] **Fargate Spot for the api task** — saves ~$12.6/mo (~70% off → ~$5.40/mo). Effort: LOW (capacity-provider change). **WAIT-UNTIL-LAUNCH** (interruption risk); fine for dev now.
- [ ] **Pause whole stack when not actively developing** (scale Fargate desired→0, stop/pause Aurora, optionally delete NAT) — saves ~$5-6/day while idle. Effort: LOW (a stop/start script). **SAFE-NOW** for pre-launch — this is the single biggest budget protector before launch.
- [ ] **Consolidate Secrets Manager: merge the 6 friesenlabs/platform/shared/* + uplift/* into fewer JSON secrets, or move low-sensitivity config to SSM Parameter Store (free)** — saves ~$2-3/mo. Effort: MEDIUM (code refs to update). **SAFE-NOW** but low priority vs the infra levers.
- [ ] **Set CloudWatch Logs retention on /aws/rds/cluster/uplift-aurora/postgresql (currently NEVER-EXPIRE) to 30d** — saves $0 now, prevents unbounded future storage growth. Effort: LOW. **SAFE-NOW** (preventive).
- [ ] **Switch CloudFront PriceClass_All → PriceClass_100 (NA+EU)** — $0 now, lowers future per-GB/request once traffic starts (if audience is US/EU). Effort: LOW. **WAIT-UNTIL-LAUNCH** (only matters with traffic).

_Note: Aurora `min_capacity=1` is deliberate (HNSW index builds starve at 0.5) — use auto-pause-to-0-when-idle, not a 0.5 floor._


## AI-plane readiness — when Anthropic Managed Agents creds arrive

The MA beta API exists today as assumed (`managed-agents-2026-04-01`); model ids + pricing verified. 4 reworks needed before live (none blocked on the API):

- [ ] Hoist per-tenant provisioning out of the request path — `conv/session.py` calls `coordinator.build()` (env+7 agents+coordinator) on every Conversation; persist `coordinator_id`/`environment_id` per tenant (the 'agent once' fix).
- [ ] `send_message`: replace the sync dict facade with the real event-stream flow (stream→send→drain-to-idle).
- [ ] `create_session`: pass the now-persisted `environment_id` (currently never persisted).
- [ ] Worker tool adapter: wrap repo `Tool` ABCs as the SDK `BetaFunctionTool` type (preserve ALWAYS_ASK→Greenlight).


**Open questions needing a decision before AI onboarding:**

- [ ] HIPAA fallback runtime: CLAUDE.md says HIPAA tenants use a Bedrock/1P fallback via the runtime.py seam — but Managed Agents is NOT available on Amazon Bedrock (only first-party API + Claude Platform on AWS). What does the HIPAA path actually run? If it needs the agent loop, it can't be MA-on-Bedrock; it would have to be Claude API + tool use (self-hosted loop) or MA on Claude Platform on AWS (which does NOT support self_hosted sandboxes). This needs a concrete decision before a HIPAA tenant onboards.
- [ ] 100-workspace ceiling: the one-workspace-per-tenant model breaks past ~100 tenants. Is the target tenant count under 100, and if growth is expected, is the strategy multiple Anthropic orgs, or a coarser isolation boundary (shared workspace + per-session vault/metadata)?
- [ ] Custom-tool execution path: does Uplift run tools via the self-hosted EnvironmentWorker (no agent.custom_tool_use round-trip) OR as client-side custom tools (orchestrator handles agent.custom_tool_use over SSE)? The repo conflates both — roster lists tools as custom names while the worker executes them as environment-worker tools. One path must be chosen consistently before wiring.
- [ ] self_hosted resource limitations: self_hosted sandboxes do NOT support memory stores and do NOT mount file/github_repository resources Anthropic-side (you pass pointers via session metadata and fetch them yourself). Does any Uplift agent flow assume Anthropic-side file/repo mounting or memory stores? If so it must move to the self_hosted pointer-passing pattern.
- [ ] tools callable invocation cadence: the workers_polling heartbeat (worker.py:63-67) assumes EnvironmentWorker calls the tools callable once per poll iteration. If the SDK caches the callable result instead, the per-poll heartbeat won't fire and the worker-absent alarm semantics break — needs runtime verification against the live SDK.
- [ ] environment key generation: the env key (sk-ant-oat01-…) is Console-only even when the environment is API-created. Is there an operational runbook for generating + rotating it into Secrets Manager, given the rest of provisioning is automated/Terraform?

## Next-chunk loop prompts (paste-ready)


### ai-plane — _Anthropic Managed Agents creds (org API key + beta access) — every ManagedAgentsRuntime method raises NotImplementedErro_

```
/loop Wire the Uplift AI/agent plane end-to-end for a real chat round-trip. Work in dependency order — do NOT skip a step whose inputs do not yet exist. For each step: implement, write tests, verify they pass, then move on.

SEQUENCE:

1. Read and verify MA beta SDK shapes (no code). Confirm client.beta.environments / client.beta.agents / client.beta.sessions / client.beta.vaults paths and the managed-agents-2026-04-01 header against current SDK docs. Record discrepancies. BLOCKED until Anthropic creds — author only.

2. Amend /Users/yee/Desktop/friesenlabs/db/schema.sql: add tenant_workspaces table (tenant_id uuid PK, environment_id text, coordinator_id text, workspace_id text, created_at). Add it to the tenant_tables array in the existing DO block so FORCE RLS is applied. Add explicit ENABLE+FORCE statements below (belt-and-suspenders). Verify: run the SQL through libpg_query (pytest tests/unit/test_sql_schema.py must stay green).

3. Create /Users/yee/Desktop/friesenlabs/agents/workspace_store.py with InMemoryWorkspaceStore and PgWorkspaceStore (psycopg2, lazy import, SET app.current_tenant before every query — same pattern as api/control/greenlight.py PgApprovalStore). Write tests/unit/test_workspace_store.py. Verify: pytest tests/unit/test_workspace_store.py passes; import is network-free.

4. Implement the five ManagedAgentsRuntime methods in /Users/yee/Desktop/friesenlabs/agents/runtime.py using the verified SDK shapes. Keep BLOCKED/VERIFY comments. Keep FakeRuntime and get_runtime() unchanged. Verify: pytest tests/unit/test_runtime_adapter.py still passes; python -c 'from agents.runtime import ManagedAgentsRuntime' is import-safe.

5. Create /Users/yee/Desktop/friesenlabs/api/pg_clients.py with PgRagClient (pgvector cosine search, SET app.current_tenant first) and PgCrmClient (allow-listed table reads, set_tenant, find_companies, find_contacts). Write unit tests mocking psycopg2. Verify: SET before SELECT invariant is tested; import-safe.

6. Create /Users/yee/Desktop/friesenlabs/conv/synthesizer.py with AnthropicSynthesizer (messages.create with MA beta header, parse claims, filter source_refs to retrieved set). Write unit tests with mocked Anthropic client. Verify: citation invariant holds in tests; import-safe.

7. Wire /Users/yee/Desktop/friesenlabs/api/asgi.py: replace conversation_factory=lambda tenant_id: None with a real factory using PgWorkspaceStore + ManagedAgentsRuntime + PgRagClient + PgCrmClient + AnthropicSynthesizer + Greenlight. Replace executor=lambda action: {'status':'noop'} with agents.tools.registry.resolve(action.name).invoke(ToolContext(...), **action.payload). Verify: pytest tests/integration/test_asgi_prod.py passes; add test_chat_503_when_workspace_not_provisioned.

8. Do NOT edit infra/** (LANE NICK territory — see CONTRIBUTING.md two-lane contract). Append the needed IaC as a fenced HCL spec to infra/REQUESTS.md: uplift/env-id secret, worker task-def env (UPLIFT_ENV_ID + DB_USER/DB_PASS + CLOUDWATCH_METRICS), main.tf variable wiring with safe "" defaults. LANE NICK authors, validates, applies.

9. Update /Users/yee/Desktop/friesenlabs/worker/worker.py: in run(), build PgCrmClient + PgRagClient + PgApprovalStore-backed Greenlight from env vars; pass to build_context(). Verify: python -c 'from worker.worker import run' is import-safe; existing tests pass.

10. Update /Users/yee/Desktop/friesenlabs/signup/provisioning.py: add workspace_store=None to __init__; after agent_plane.ensure() call workspace_store.upsert(tenant_id, env_id, coordinator_id, workspace_id). Update api/prod_deps.py _Noop agent_plane.ensure() to return {'environment_id':'stub','coordinator_id':'stub'}. Verify: pytest tests/unit/test_signup_provisioning.py passes with new assertion that workspace row is written.

11. Create /Users/yee/Desktop/friesenlabs/tests/integration/test_agent_plane_live.py gated on ANTHROPIC_MANAGED_AGENTS_TEST env var. Tests: create_environment, coordinator.build, create_session, send_message, conversation round-trip via InMemoryWorkspaceStore seeded with a live coordinator_id. Verify: all tests skip cleanly without creds; pass with creds.

VERIFY LIKE A SKEPTIC after every step:
- pytest full suite must stay green (currently 193 passed / 2 skipped).
- terraform validate clean after IaC changes.
- python -c 'import api.asgi' must not crash (import-safe).
- RLS: confirm SET app.current_tenant is called before every DB query in the new code.
- THE TRUST RULE: tenant_id comes only from the verified JWT claim in api/auth.py make_current_tenant; never from request body or a header.
- Org API key only in API Fargate task; never in worker task def.

PR: branch feat/agent-plane-live, short-lived, squash-merge to main. Update README.md, CLAUDE.md, BUILD_STATUS.md (check off the AI plane items), TODO.md (mark the P0 agent-plane tasks done) on every commit.
```

### provisioning — _All real production calls are BLOCKED: needs Nick. Specifically: terraform apply for infra/modules/secrets/main.tf new r_

```
/loop You are implementing the real signup/provisioning stack for Uplift. The codebase is at /Users/yee/Desktop/friesenlabs. Read CLAUDE.md before any change. This is a code-only chunk — no terraform apply, no real sends, no live AWS mutations.

WHAT TO BUILD (in dependency order):
1. db/schema.sql ONLY, append-only — add `accounts` table (pre-tenant, no tenant_id RLS filter, carry an `-- RLS-EXEMPT (pre-tenant): <reason>` comment) and `stripe_events` idempotency ledger. Do NOT edit db/roles.sql (LANE NICK): append the needed GRANT crm_app DML statements as a fenced SQL block to infra/REQUESTS.md; tests GRANT in fixtures.
2. signup/tokens.py — HMAC-SHA256 signed email tokens + random SMS OTP with TTL and constant-time verify.
3. signup/store_pg.py — PgAccountStore (get/get_by_email/insert/update over accounts table, crm_app DSN, psycopg2); PgStripeEventLedger (is_handled/mark_handled with ON CONFLICT DO NOTHING); PgOtpStore (OTP stored in accounts.meta jsonb with atomic merge).
4. signup/cognito_admin.py — CognitoAdminClient wrapping boto3 cognito-idp for admin_create_user (MessageAction=SUPPRESS), admin_update_user_attributes (custom:tenant_id), admin_confirm_sign_up. Idempotent. VERIFY comments on confirm() call sequence.
5. signup/stripe_adapter.py — StripeAdapter over stripe library. Plan->Price ID via injected dict. Add stripe>=7 to requirements-api.txt.
6. signup/resend_sender.py — ResendEmailSender over Resend REST API (urllib.request). send_verification (signed link) + send_welcome. Logs but does not raise on send failure.
7. signup/sms_sender.py — SnsSmsOtpSender via boto3 SNS. SmsSendError exception.
8. signup/anthropic_admin.py — AnthropicAdminClient. Mark every endpoint URL with VERIFY comment. Implements ensure_workspace (idempotent), create_workspace_key, set_limits, delete_workspace.
9. signup/lambda_handler.py — Lambda handler for the Step Functions provisioning machine. Builds clients from env on cold start. Calls Provisioner.provision() (idempotent). Handles activate and park_failed as state-only flips.
10. api/prod_deps.py — replace all _Stub/_Noop with real clients behind env guards. Add SfnProvisioningTrigger (start_execution keyed by account_id). Keep stubs as fallback when env vars absent so /healthz still boots without creds.
11. signup/payment.py — add optional event_ledger to PaymentService. Check ledger before state mutation; mark after.
12. api/signup_routes.py — add GET /signup/{account_id}/verify-email?token={token} for email click-through; 303 redirect to SPA on success.
13. Do NOT edit infra/** (LANE NICK territory — see CONTRIBUTING.md two-lane contract). Append the full IaC spec as fenced HCL to infra/REQUESTS.md: 6 new SM secrets + outputs; api task-def secret vars + secrets[] + env vars; IAM cognito-admin-ops + sfn-start-execution policies on the api task role; aws_lambda_function for provisioning + scoped IAM. New env/secret NAMES land in shared/config.py first. LANE NICK authors, validates, applies.
14. Tests: tests/unit/test_tokens.py (9 tests), tests/unit/test_pg_account_store.py (6 tests), tests/unit/test_stripe_adapter.py (5 tests), tests/unit/test_cognito_admin.py (4 tests), tests/integration/test_provisioning_e2e.py (3 tests, skipped without STRIPE_TEST_SECRET_KEY). All existing 193 tests must still pass.
15. Living docs: update README.md (new env vars), BUILD_STATUS.md (provisioning adapters now real, blocked items updated), TODO.md (check off completed items), CLAUDE.md (update parked/not-live list).

HARD CONSTRAINTS (from CLAUDE.md):
- THE TRUST RULE: custom:tenant_id comes ONLY from the verified Cognito JWT custom:tenant_id claim. CognitoAdminClient.set_tenant_id uses admin-only IAM, never the client app.
- Provisioning fires ONLY on the signed Stripe webhook. The SfnProvisioningTrigger replaces the synchronous on_paid call but is still invoked only from handle_webhook after construct_event succeeds.
- No live sends: all new senders (Resend, SNS, Anthropic Admin) must be gated behind env vars and stub/noop in offline tests.
- Secrets never in repo. Use os.environ + Secrets Manager refs only.
- RLS: the accounts table is pre-tenant (explain the exception in a comment); all other tables keep FORCE RLS.
- VERIFY before any Anthropic Admin API call — mark every endpoint with # VERIFY comment.
- On every commit: update README.md + CLAUDE.md + BUILD_STATUS.md + TODO.md to reflect what changed.

VERIFY LIKE A SKEPTIC after each step:
- python -c 'from signup.X import Y' exits 0 (import-safe, no network on import)
- pytest --ignore=.venv — 193 + N passed, same 2 skips, no new failures
- cd infra && terraform validate — 0 errors across all 19 modules
- grep -r 'STRIPE_SECRET\\|RESEND_API_KEY\\|ANTHROPIC_ADMIN' **/*.py shows only os.environ reads, never hardcoded values
- grep 'custom:tenant_id' signup/cognito_admin.py confirms admin-only attribute set

When done, open a PR with title 'feat/provisioning-real-clients' on branch feat/provisioning-real-clients targeting main.
```

### cube-worker-deploy — _UPLIFT_ENV_KEY and UPLIFT_ENV_ID for the worker service require Anthropic Managed Agents environment provisioning (creat_

```
/loop Deploy cube + worker Fargate services with digest-pinned arm64 images. No new external credentials are needed for the build/push/cube-apply sequence; only UPLIFT_ENV_KEY (Anthropic MA env key) blocks the worker from being functional.

Sequence, verify like a skeptic at each step:

1. MODIFY infra/modules/cube/main.tf — add `variable "image"` (default ""), add `runtime_platform { cpu_architecture="ARM64" operating_system_family="LINUX" }` to aws_ecs_task_definition.cube, change hardcoded `cubejs/cube:latest` to `var.image != "" ? var.image : "cubejs/cube:latest"`, add env `CUBEJS_SCHEMA_PATH=/cube/conf`. Wire `image = var.cube_image` in infra/main.tf module.cube block.

2. MODIFY infra/modules/worker/main.tf — add `runtime_platform { cpu_architecture="ARM64" operating_system_family="LINUX" }` to the worker task def. Add plaintext env vars: `UPLIFT_ENV_ID` (from new var `uplift_env_id`), `CLOUDWATCH_METRICS=1`, `AWS_REGION` (from var.region), `CUBE_ENDPOINT` (from new var `cube_endpoint`). Wire in main.tf. Add `variable worker_image` and pass as `image` to module.worker.

3. ADD `variable cube_image`, `variable worker_image`, `variable uplift_env_id` (default ""), `variable cube_endpoint` (default "") to infra/variables.tf.

4. ADD Cloud Map service discovery to infra/modules/cube/main.tf: `aws_service_discovery_private_dns_namespace` "uplift.local" + `aws_service_discovery_service` "cube" + `service_registries` on aws_ecs_service.cube. Output the DNS name. Pass `cube_endpoint = module.cube.service_dns_name` in main.tf.

5. ADD cloudwatch:PutMetricData inline policy to the worker task role in infra/modules/iam/main.tf (resource aws_iam_role_policy "worker_task_cloudwatch").

6. PIN Dockerfiles: resolve arm64 digest for `python:3.13-slim` and current stable digest for `cubejs/cube`, substitute in worker/Dockerfile and semantic/Dockerfile FROM lines.

7. BUILD + PUSH (requires Nick/AWS creds):
   - docker build --platform linux/arm64 -t 186052668426.dkr.ecr.us-east-1.amazonaws.com/uplift-cube:v1.0.0 -f semantic/Dockerfile semantic/
   - docker push ...; capture sha256 digest
   - docker build --platform linux/arm64 -t 186052668426.dkr.ecr.us-east-1.amazonaws.com/uplift-worker:v1.0.0 -f worker/Dockerfile .
   - docker push ...; capture sha256 digest

8. SET in infra/prod.auto.tfvars (gitignored): cube_image and worker_image using @sha256:<digest> URIs.

9. POPULATE Secrets Manager uplift/cube-api-secret with `openssl rand -base64 32`. (env-key BLOCKED: needs Nick — MA creds.)

10. VALIDATE then APPLY (requires Nick/AWS apply authorization):
    - terraform validate (must be 0 errors, 19+ modules)
    - terraform plan (must show only adds for cube+worker services, 0 change/destroy to live)
    - terraform apply

VERIFY like a skeptic:
- After apply: `aws ecs describe-services --cluster uplift-cluster --services uplift-cube uplift-worker` → runningCount=1 each
- Cube health: curl from within VPC to the Cloud Map DNS `http://uplift-cube.uplift.local:4000/livez` → 200
- Worker metric: `aws cloudwatch get-metric-statistics --namespace Uplift/Agents --metric-name workers_polling ...` → datapoints with Value=1 within 60s (only verifiable once env-key is populated)
- ECR immutability: attempt docker push with the same tag → must be rejected (IMMUTABLE)
- Trust rule: confirm cube security context — a Cube REST query without a signed JWT carrying tenant_id is rejected by queryRewrite with "no tenant"

PR: open feat/deploy-cube-worker, update BUILD_STATUS.md (cube/worker status: applied), update TODO.md (check off the cube and worker deploy items), update CLAUDE.md parked section.

BLOCKED items (do not attempt): worker full functionality until Nick provides Anthropic MA env-key + env-id. Mark worker service applied-but-parked in BUILD_STATUS.
```

### observability-cicd — _The OIDC IAM identity provider for GitHub Actions and the deploy IAM role (DEPLOY_ROLE_ARN) must be created by Nick befo_

```
/loop /loop Operations deployment chunk for Uplift — observability, guardrails fix, WAF, origin-secret, Aurora hardening, CI/CD pipeline.

## Scope
Implement the following changes, in dependency order. Read every file before editing it. Do not apply terraform. Do not run docker build or push. Author code and IaC only.

## Files to create or modify

1. `infra/modules/guardrails/main.tf` — Add `variable "notify_topic_arn"` (type=string, default=""). Add `alarm_actions = var.notify_topic_arn != "" ? [var.notify_topic_arn] : []` to `aws_cloudwatch_metric_alarm.billing`. This wires the billing alarm to the shared SNS topic so someone actually gets paged.

2. `infra/modules/data/main.tf` — In `aws_rds_cluster.this` add: `backup_retention_period = 7`, `deletion_protection = true`, `skip_final_snapshot = false`, `final_snapshot_identifier = "${var.project}-aurora-final"`, `copy_tags_to_snapshot = true`. In `aws_rds_cluster_instance.this` add: `performance_insights_enabled = true`, `performance_insights_retention_period = 7`.

3. `infra/modules/secrets/main.tf` — Add `aws_secretsmanager_secret.origin_verify` (name=`uplift/origin-verify-secret`). Add `output "origin_verify_secret_arn"`.

4. `infra/variables.tf` — Add `variable "x_origin_verify_secret"` (type=string, sensitive=true, default="", description="Shared secret for CloudFront→ALB X-Origin-Verify header").

5. `infra/modules/api_cdn/main.tf` — Add `variable "x_origin_verify_secret"` (sensitive=true, default=""). In the `origin` block add a dynamic `custom_header` block (for_each on secret != ""; name=X-Origin-Verify; value=var.x_origin_verify_secret). Add `aws_wafv2_web_acl.api` (scope=CLOUDFRONT; rules: AWSManagedRulesCommonRuleSet priority=1, AWSManagedRulesKnownBadInputsRuleSet priority=2, rate-based limit=2000/IP priority=3 action=block). Set `web_acl_id = aws_wafv2_web_acl.api.arn` on the distribution.

6. `infra/modules/alb/main.tf` — Add `variable "x_origin_verify_secret"` (sensitive=true, default=""). Add `aws_lb_listener_rule.origin_verify` (count = secret != "" ? 1 : 0; listener=http_forward[0].arn; priority=1; action=forward to target group; condition=http_header X-Origin-Verify == secret). Keep the existing http_forward default_action as-is (forward, not 403) so that count=0 in CI validate mode keeps CI green. The rule at priority=1 takes precedence over the default forward action for requests WITH the header; requests WITHOUT the header still reach the default action. Note: this means requests without the header still get forwarded in the initial deploy. A separate follow-up (after verifying CloudFront always sends the header) changes the default_action to fixed-response 403.

7. `infra/main.tf` — Four changes: (a) Add `x_origin_verify_secret = var.x_origin_verify_secret` to module.api_cdn. (b) Add `x_origin_verify_secret = var.x_origin_verify_secret` to module.alb. (c) Add `notify_topic_arn = module.observability.alarms_topic_arn` to module.guardrails. (d) Confirm (read before modifying) that `notify_email = var.notify_email` is already in both module.observability and module.guardrails — it is, at lines 155 and 163 respectively.

8. `.github/workflows/deploy.yml` — Create this file. Trigger: `on: push: tags: ['v*.*.*']`. Jobs in order: (a) `build-push` — OIDC configure-aws-credentials (role=DEPLOY_ROLE_ARN secret), docker/setup-buildx, docker/login to ECR, docker/build-push with platforms=linux/arm64, file=api/Dockerfile, tag=${{ secrets.ECR_REPO }}:${{ github.sha }}, push=true. Output image_tag=${{ github.sha }}. (b) `tf-plan` — needs build-push. setup-terraform@v3. terraform init -chdir=infra with -backend-config flags from secrets TF_BACKEND_BUCKET, TF_BACKEND_KEY, TF_BACKEND_REGION. terraform plan -chdir=infra -var="api_image=${{ secrets.ECR_REPO }}:${{ needs.build-push.outputs.image_tag }}" -var="x_origin_verify_secret=${{ secrets.X_ORIGIN_VERIFY_SECRET }}" -var="notify_email=${{ secrets.NOTIFY_EMAIL }}" -out=tfplan -no-color | tee plan.txt. Upload plan.txt and tfplan binary as artifacts. (c) `tf-apply` — needs tf-plan. environment: production (manual approval). Download tfplan artifact. terraform init (same). terraform apply -chdir=infra tfplan. (d) `deploy-ecs` — needs tf-apply. aws ecs update-service --cluster uplift --service uplift-api --force-new-deployment. aws ecs wait services-stable --cluster uplift --services uplift-api. (e) `deploy-amplify` — needs tf-apply. aws amplify start-job --app-id ${{ secrets.AMPLIFY_APP_ID }} --branch-name main --job-type RELEASE. Poll for SUCCEED/FAILED with a 5-minute timeout.

## Verification after each step

After all edits, run:
```
terraform -chdir=infra fmt -check -recursive   # must pass (CI gate)
terraform -chdir=infra init -backend=false
terraform -chdir=infra validate                 # must pass (CI gate)
```

Then update the living docs:
- `TODO.md` — check off: "Close the ALB origin", "Put a WAFv2 web ACL", "Deploy the observability module", "Fix the billing alarm/budget notification", "Build a real CI/CD deploy pipeline", "Raise Aurora backup retention + deletion protection".
- `BUILD_STATUS.md` — update the unapplied modules list to reflect observability/guardrails/WAF/origin-verify are now authored+wired (apply still needs Nick for the first run, but the code is ready).
- `CLAUDE.md` — remove observability from the parked/unapplied list in the bullet at line 29.
- `README.md` — add WAF, alarms, shared-secret origin to the live infra description.

## Constraints (MUST follow)
- No `terraform apply`, no `docker build`, no AWS CLI calls that mutate live state.
- Secrets never in the repo. The x_origin_verify_secret value goes in the gitignored prod.auto.tfvars only.
- ECR tags: the deploy.yml must use `github.sha` as the image tag, never `:latest`.
- WAFv2 scope=CLOUDFRONT must be in the same provider as CloudFront (us-east-1, which is the only provider configured).
- `terraform validate` must remain clean after every file edit — check before moving to the next file.
- CI (.github/workflows/ci.yml) runs on push to main and PRs; deploy.yml runs only on semver tags. They must not conflict.
- Update README, CLAUDE.md, BUILD_STATUS.md, TODO.md on the final commit per the repo convention.
```
